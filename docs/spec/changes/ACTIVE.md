# 活跃变更：子系统占用账本

> 规则见 [`.claude/rules/parallel-development.md`](../../../.claude/rules/parallel-development.md)。
> **一个子系统同一时刻只允许一个 in-flight change 持有。** 开 change（阶段 2）前查此表，被占则排队；归档（阶段 9）后释放。
> `docs` 不上锁（见协议）。

> **~~fix-imported-free-func-namespace~~** ✅ 已归档 2026-07-27（隔离 worktree 预抢，User 授权；
> compiler 根因 fix：导入自由函数跨包裸调误限定当前 ns → 补 `ImportedSymbols.FunctionNamespaces` +
> `EmitContext.QualifyFreeFunc` + `IrDump._filterShadowedFuncs`，与类 `ImportedClassNs` 对称。
> 自举不动点 5/5 gen1==gen2 + stdlib 25/25 + 新 cross-zpkg `free_func_cross_pkg`（旧 z42c 复现 undefined /
> 新 z42c 25·36）。字节不动点安全：全 compiler+stdlib 0 个跨包裸调自由函数。解锁 `add-repl-decls-multiline`。
> 与 `nested-types-followup`(compiler 锁持有者) 物理隔离；archive：`archive/2026-07-27-fix-imported-free-func-namespace`）。

> **独立分支 in-flight（2026-07-14，User 授权直接推进，不排队）**：`stabilize-dispatch-keys`
> （方案A：派发键一律全签名 mangle + `Path.Join`/`String.Join` 落地 `params`；占 `compiler`+`ir`+
> `runtime`+`stdlib`）在分支 `claude/compiler-params-join-solution-ie04pf` 上开发，与主线 change
> （`split-irgen-class` 等）物理隔离，合并时按常规解冲突。GREEN 以 CI 为权威（冷环境本地不可验自举链）。

> **~~add-repl-decls-multiline~~** ✅ 已归档 2026-07-28（User 授权隔离 worktree 预抢，扩张进
> **compiler** 子系统——原 toolchain-only）。REPL 两块 Python 对齐能力：**多行输入**（`ReadBlock` 接线）
> + **函数/类型声明累积**（声明入 `Repl.R{N}` ns、`ExtendWithPackage` 并入 CachedScan + 后续轮 `using`
> 解析，零壳类/零改写；重定义 ERROR）。实施期实测发现类**实例方法** + **enum** 跨轮不 resolve → 修两处
> compiler 缺口：`z42c.pipeline/DepScan.ExtendWithPackage` 把增量包并入 world（`_rebuildClass` 才能定位
> 类自身读 SIGS 方法）+ `z42.ir/TsigReconcile._rebuildModule` 导出本地 enum（此前恒排除、只留内建
> GCHandleType；**亦修一般跨包 enum 导入**）。无格式 bump。GREEN：`xtask test` 全绿（goldens 214/214 +
> stdlib 25/25 + **自举不动点 7/7 gen1==gen2** + cross-zpkg 8/8 含新 `enum_cross_pkg` + vscode-syntax）+
> canonical driver 匹配。与 `nested-types-followup`(compiler 锁持有者，main checkout) 物理隔离
> （改 DepScan/TsigReconcile 不与嵌套类型重叠），合并解冲突。archive：`archive/2026-07-28-add-repl-decls-multiline`。
> 释放 toolchain 锁。enum-export 无 byte 漂移（仅影响真正 use 跨包 enum 的代码；zbc-format freeze 夹具的
> `__box_prim` 漂移是 pre-existing boxing 陈旧、已还原、非本 change）。

> **~~add-z42-repl~~** ✅ 已归档 2026-07-27（0.4.0 capstone：z42 原生交互式 REPL——VM rustyline
> 行编辑 + `__load_bytecode_in_memory` builtin ‖ 新库 `z42.scripting`（Form B `Script.Eval`，物理
> 落 `src/toolchain/scripting/`）‖ `z42.interactive`(z42i) 宿主 + launcher `repl` 路由。发货实现把
> 计划的多文件结构收敛进 `Script.z42`+`Rewriter.z42`+`interactive_main.z42`；MVP（表达式/var
> carry-forward/using/语句/错误恢复/`.help .exit .quit .vars .usings`/`-c`）端到端已验证。**多行输入
> + 函数/类型声明累积显式延后给 `add-repl-decls-multiline`**（本次开工的后继 change）；富元指令集 +
> 富结果格式化留 follow-up。archive：`archive/2026-07-27-add-z42-repl`，释放 runtime+stdlib+toolchain
> 锁。perf 收口见已归档 `perf-optimize-repl-eval`(#46 每轮 3.5s→70ms)。

> **独立分支 in-flight（2026-07-24，User 授权并行推进，不排队）**：`infer-var-field-types`
> （编译器根因 fix：`static var` 字段跨 zpkg 导出丢推断类型 → 消费端 E0402；双通道修——
> `VarFieldInfer` fixup pass 回写 `fs.FieldType`(TSIG) + `ClassDescBuilder` var 字段用推断类型
> (TYPE 段)；占 `compiler`）在分支 `claude/infer-var-field-types` 上开发，与 `split-irgen-class`
> 物理隔离，合并时解冲突。**无回归依据**：只改 var 字段路径，z42c 源 0 处 public static var，
> A/B 证 z42.collections 字节不变。`add-z42-repl` 的 carry-forward 状态模型依赖它。GREEN 以 CI
> 为权威。spec：`docs/spec/changes/infer-var-field-types/`。

## 子系统持有表

| 子系统 | 当前持有 change | 起始 | 说明 |
|--------|----------------|------|------|
| `compiler` | `nested-types-followup` | 2026-07-25 | 🔴 IMPL（嵌套类型补完：base/接口为兄弟或祖先嵌套类型 → `NestedFlatten` 在 AST 上把嵌套 base/接口引用限定到 `+` 全名；继承字段 + 虚派发 + 上转型 + 嵌套接口 impl/GetInterfaces 全通。占 compiler）。**~~add-nested-types~~ ✅ 已归档 2026-07-25（#38 合并 → `archive/2026-07-25-add-nested-types`，释放 compiler+runtime 锁）**：嵌套类型作语言特性 + 反射（FQ `+` 分隔、无 format bump）。**~~split-irgen-class~~ ✅ 已关闭（User 授权 2026-07-25：不再处理，释放 compiler 锁）**——历史：🟡 refactor（review P1-3：拆 IrGen God-Class，同 P1-1/2 手法）。**~~fix-imported-generic-method-return~~** ✅ 已归档 2026-07-25（**PR #34 合并，短占 compiler 锁已释放**；User 授权预抢——隔离 worktree `z42-genret-wt`；修导入泛型「方法返回 / 字段访问轴」类形参不替换：`List<string>.ToArray()` 跨包返回未替换 `T[]`→E0402。`Z42ClassType.GenericParamNames`（本地 AST + 导入 TSIG）两处回填 + `MemberResolver._substGeneric` 按 receiver TypeArgs 递归替换（裸 T / T[] / 嵌套）。与 #30「导入泛型字段字面类型」同族、成员访问/方法返回轴。GREEN：old z42c→E0402 复现、gen1 编过并运行正确、**自举不动点 gen1==gen2 byte-identical**（semantics/pipeline/driver）、全 25 stdlib gen1 编译通过无 miscompile；CI 主流水 0 失败（compile-toolchain 不动点 + test-stdlib interp×6/jit×4 + cross-zpkg 全绿）。bench-pr 跨-runner 基线漂移误报（5 bench 全 byte-identical、04 −46% 反证），非本 change。收尾 PR：释放锁 + 移除 add-z42-repl 的 ScriptState ToArray 绕过。归档即还锁）。**~~fix-benchmark-bencher-arg-trampoline~~** ✅ 已归档 2026-07-20（短占 compiler 锁，User 授权预抢——隔离 worktree `z42-bench`；把 `BenchmarkDesugar` pass 移植回 z42c：`[Benchmark] void f(Bencher b)`（form-2）AST-desugar 成 demoted `f$impl(Bencher b)` + 零参 `[Benchmark]` wrapper（`Bencher b=new Bencher(); f$impl(b); b.printSummary("f")`）。该 pass 原是 C# bootstrap 实现，随 `f8ff73d5 删 C# 编译器`删除、**z42c 从未移植** → form-2 benchmark 自切换起一直运行期失败（`MethodInfo.Invoke: expects 1 argument, got 0`）；add-stdlib-bench-baseline 实施期发现 3 个 pre-existing form-2 失败 → 本 fix 根治。挂钩 4 处 `AttributeSynth.Run(X)`→`AttributeSynth.Run(BenchmarkDesugar.Run(X))`（IncrementalDriver ×1 + IrDump ×3）。GREEN：test compiler **22 单元全绿（含 6 新 desugar 单测）+ 自举不动点 7/7 gen1==gen2 byte-identical**（z42c 源无 form-2 → 零扰动）；3 个 form-2 基准端到端 PASS、`bench stdlib` 全绿。踩坑：`impl` 是保留关键字误作变量名（两处），被前一 change fix-parse-diags-dropped 的关键字诊断当场抓出。完整 gate 以 CI 为权威。归档即还锁）。**~~fix-parse-diags-dropped~~** ✅ 已归档 2026-07-19（短占 compiler 锁，User 授权预抢——隔离 worktree `z42-bench`；修 z42c 前端两缺陷：① parse 诊断被整体丢弃（各编译点只为 TypeChecker 新建 bag）→ 畸形 parse 垃圾 AST 流入 codegen、typecheck 不报时**静默 miscompile**（实测 `string module` 参数名致方法被无声吞掉零报错）；② 关键字占参数名/成员名位置无精准诊断、不消费 token 致解析脱轨级联垃圾错。修复：Parser 挂 DiagnosticBag 到 CU（`CompilationUnit.ParseDiags` + `MergeParseDiags`）+ 7 个 typecheck 汇点合并上浮；MemberParser 名字位置关键字 → `cannot use keyword 'X' as a parameter/member name` + 消费防脱轨（`_isWordKeyword` TokenKind 9..93+true/false/params）。rebuild-bench-structured-output 实施期发现（原判「数组参数误解析/多参 brace-body 丢导出」两 bug，探针收敛为同一根因）。GREEN：test compiler 全绿（20 单测文件 + 2 新回归测试 + e2e 6/6 + **不动点 7/7 gen1==gen2**）+ stdlib canary 全绿；完整 gate 以 CI 为权威。归档即还锁）。**排队**：`converge-z42c-onto-z42-project`（🔴 DRAFT，现持 stdlib 锁）、`add-partial-types`（🔴 DRAFT）。历史：**~~split-parser-class~~** ✅ 已归档 2026-07-12（review **P1-2 完成**：拆 Parser God-Class → 5 子解析器 `TypeParser`/`ExprParser`/`StmtParser`/`DeclParser`/`MemberParser` + mediator；**Parser 1743→265，6 文件全 <500**；同 P1-1 EmitContext 式 mediator（Parser 持游标 + 原语，子解析器持 `_p`）；不动点 7/7 + golden 138/138。踩坑：脚本 find_method 声明锚定 + net_braces 跳字符串/注释花括号。）历史：**~~extract-member-resolver~~** ✅ 已归档 2026-07-12（review **P1-1 完成**：拆 TypeChecker God-Class → 6 子绑定器 `OverloadBinder`/`MemberResolver`/`StmtBinder`/`ExprTyper`/`TypeFactsTc`/`DeclBinder` + 协调器 Facade；**TypeChecker 1937→257，7 文件全 <500**；EmitContext 式 mediator（TC 作 mediator + 子绑定器持 `_tc`）；不动点 7/7 + golden 138/138。含 step1 OverloadBinder（前 change split-typechecker-class 已归档）→ step2-5 本 change。）历史：**~~add-overload-resolver-tests~~** ✅ 已归档 2026-07-12（review P3-5：补 OverloadResolver.Resolve/MangleKey 8 单测，8 passed；环检测既有 topo_tests 已覆盖 → P3-5 清零）。历史：**~~split-typechecker-class~~（step 1）** ✅ 已归档 2026-07-12（review P1-1 起步：验证 EmitContext 式 mediator 拆法 → 抽 `OverloadBinder`（重载决议 9 方法，持 `_tc` 反向引用，跨界经 `_tc._bindExpr`/`_isAssignable`/`_diags`）；TypeChecker 1937→1703。不动点 **7/7 byte-identical** + golden 138/138。**P1-1 未完**：步骤 2-4（MemberResolver/StmtBinder/ExprTyper）+ 叶子助手 TypeFactsTc 转后续 change，TypeChecker 仍 >500。踩坑：脚本误覆写既有 OverloadResolver.z42 静态工具，git checkout 恢复。）历史：**~~split-typechecker-member-fns~~** ✅ 已归档 2026-07-12（review P1 续：`_bindMemberCall` **98→48** 抽 `_bindInstanceMemberCall`、`_bindMember` **80→23** 抽 `_bindInstanceMember`+`_bindClassMemberAccess`；接收者类型分派尾纯搬移、零语义变化。不动点 **7/7 byte-identical** + golden **138/138**。遗留 4 个非分派 >60 行函数归后续 review。）历史：**~~split-typechecker-fns~~** ✅ 已归档 2026-07-12（review P1：TypeChecker 分派巨函数拆逐节点/逐语句私有 binder——`_bindExpr` **278→43**（抽 15 个逐节点 binder）+ `_bindStmt` **218→38**（抽 11 个逐语句 binder）；纯分派、同类内抽子函数、零语义变化。build compiler 不动点 **7/7 byte-identical** + 聚焦 golden **83/83**（genB + flat31 清洁 stdlib）。遗留 6 个非分派 >60 行函数（_bindMemberCall/_bindMember/_bindClass/_bindLambda/_synthCtors/_resolveParamsOverload）归后续 review。）历史：**~~consolidate-object-methods~~** ✅ 2026-07-12（Object-4 单源到 z42c.ir）、**~~compiler-review-cleanup~~** ✅ 已归档 2026-07-12（review 债务：删 6 Skeleton 死文件 / StrMap 统一到 z42c.ir / Driver exit-code 契约常量；fixpoint 7/7 + GREEN）。**排队**：`converge-z42c-onto-z42-project`（🔴 DRAFT）、`add-partial-types`（测 z42.project 登记 member 是否炸自举 → 定分阶段）。**排队**：add-partial-types（等本 change 释放 compiler 锁）。历史：**~~drop-tsig-expt~~** ✅ 已归档 2026-07-11（unify **P3**——**收官**：删 zpkg 顶层 EXPT+TSIG 两段；DepScan 跨包解析改走 `TsigReconcile.Rebuild`（读 TYPE/SIGS/IMPL，`BuildWorld` 一次解析复用；indexed 经散装 zbc 读 TYPE）；`ReadImplInto` 读 IMPL→Impls（跨包 impl 传播）。zpkg 0.30→0.31、zbc 不变。**z42.core 193KB→92KB（~半）**。死代码清（ReadTsig/3 废写入器/Reconcile/Compare+14 对账函数/reconcile 单测）。切换 byte-identical（stdlib）、不动点 7/7、GREEN 全绿、cross-zpkg 4/4。整个 unify-type-metadata（P1-a→P3）完成。历史：**~~reconcile-tsig-from-runtime-sections~~** ✅ 已归档 2026-07-11（unify **P2**：`TsigReconcile.Rebuild` 从 TYPE/SIGS/IMPL 重建 ExportedModuleZ + `Compare`（归一化：internal≈public / 类型双拼写 canon / 子集语义）与 TSIG oracle 对账；driver verb `reconcile-tsig`。**全 29 包（22 stdlib + 7 z42c）0 DIFF**。**关键发现：TSIG 有损（漏报属性 private set），SIGS 更全 → P3 删 TSIG 零信息损失**。对账 gate 揪出并根因修 3 类 IrGen SIGS 精度缺陷（native 桩类型/合成点 MinArg/后备字段可见性）+ P1-d 潜伏 bug（indexed writer 漏 intern 参数名/默认值）。无格式 bump，不动点 7/7，GREEN 全绿。CI gate 布线待 toolchain 锁（roadmap Deferred）。历史：**~~add-delegate-metadata~~** ✅ 已归档 2026-07-11（unify **P1-e ②**：delegate-as-class——每 `delegate`（含泛型）emit TYPE 条目（FQ + class_flags **bit6** + TypeParams：泛型 tps 存 TYPE、Invoke 按名引用）+ 合成 `<FQ>.Invoke` 死体桩（实例/virtual/源拼写参数+P1-d 元数据；真实调用走 CallIndirect）。`Type.IsDelegate` + Invoke 签名反射；typeof(delegate) → Deferred（ResolveTypeP 不动）。zbc 1.25→1.26 / zpkg 0.29→0.30（bit6 无 payload，沿 1.19 语义扩展先例）。两代自举 + 不动点 7/7 + GREEN 全绿 + cargo 782+21。P3 删 TSIG delegate 表的硬前置（TSIG 携带含泛型内建 11 delegate）。）历史：**~~add-param-metadata~~** ✅ 已归档 2026-07-10（unify **P1-d**：参数元数据进 SIGS——每函数 `min_arg:u16`+`params_from:u8`（method_flags 后）、每参 `name_str_idx:u32`+`default_kind:u8+payload`（0=无/1=null/2=i64/3=f64bits/4=bool/5=str）→ `ParameterInfo.IsOptional/IsParams/DefaultValue` + `Name` **权威化**（SIGS 优先，DBUG 回退）；zbc 1.24→1.25 / zpkg 0.28→0.29。**User 扩 scope**：DefaultValue 值编码并入（原拟 P1-d2）——IrGen `_fillParamMeta` 折字面量（Int/Float/Bool/String/Char/null），非字面量 kind=0 → Deferred `fold-nonliteral-param-defaults`。5 发射点全覆盖（normal/extern/abstract/impl/自由函数）。两代自举 0.28→0.29 + 不动点 7/7。**Scope 外发现**：`as string` 对 boxed str 返 null（as_cast 只认 Object/Array/Null，pre-existing VM 缺口）→ 测试用 ToString 对账，缺口留独立 change。GREEN：xtask test 全绿 + cargo lib 766+21；regen 全套 fixtures/golden/pins 25/29。TSIG P3 删。）历史：**~~add-method-modifiers~~** ✅ 已归档 2026-07-10（unify **P1-c**：方法 virtual/abstract 进 SIGS `+method_flags:u8` → `MethodInfo.IsVirtual`**权威化**（从 vtable 启发式收敛到声明 flag）/`IsAbstract`；zbc 1.23→1.24 / zpkg 0.27→0.28。**User 扩 scope**：实例 abstract 方法此前无 body 不 emit → IrGen `_emitAbstractStub` 发 signature-only 死体桩进 SIGS/FUNC 使 IsAbstract 可观测。**两踩坑**：①`MethodDecl.RetType` 是 `TypeExpr` 非 string，stub 直塞污染 STRS 池崩（须 `ResolveType().Name()`）；②z42 全方法入 vtable → `flag∨vtable` 误报非虚为 virtual（改 `sig_found?flag:vtable`）。两代自举 0.27→0.28（gen1-stdlib EMPTY Z42_LIBS）+ 不动点 7/7。GREEN：xtask test 全绿（e2e 197/0 + cross-zpkg + stdlib 含 method_modifiers/override [Test] + compiler/f5 + vscode）+ cargo lib 782+21/zbc_compat；regen zbc-format 6 + zpkg-format 4 + golden hex + Rust pinned 24/28。TSIG P3 删。）历史：**~~add-member-visibility~~** ✅ 已归档 2026-07-10（unify **P1-b**：字段/方法可见性进 TYPE/SIGS 每成员 +`visibility:u8` + 反射 FieldInfo/MethodInfo `IsPublic`/`IsPrivate`；zbc 1.22→1.23 / zpkg 0.26→0.27。**关键踩坑**：只改 writer 忘 z42c 读侧（`ZbcReader` 字段块/SIGS + `ZpkgReader.ReadModuleSigs`）→ gen1-stdlib 编 z42.encoding 游标错位崩（array index out of bounds）——**非 gated 格式：writer + 全部 reader（Rust + z42c 双 reader）必须同提交对称改**（P1-a enum 是 gated，旧 zpkg 无 block 读侧跳过，故没暴露此坑）。两代自举 0.26→0.27，**gen1-stdlib 用 EMPTY Z42_LIBS**（新 reader 只碰 0.27 兄弟 per-member dist，运行期 0.26 stdlib 只经 entry-dir 供 VM）；不动点 7/7 gen1==gen2。GREEN：xtask test 全绿（e2e 197/0 + cross-zpkg 3/0 + stdlib 含 reflection vis [Test] + compiler 单测/f5 golden + vscode）+ cargo lib 782+21/zbc_compat 3；regen zbc-format 6 + zpkg-format 4（packed×2/indexed/sym-only=.zsym）+ golden hex empty/f5/selfcheck header + zpkg header pin + Rust pinned 23/27 + expected.json。TSIG 可见性 P1 不删（P3）。）历史：**~~add-enum-type-metadata~~** ✅ 已归档 2026-07-09（unify-type-metadata **P1-a**：enum 作 TYPE 段类型实体——IrGen 加 EnumDecl 分支建 enum IrClassDesc（FQ 名 + Flags bit5 + 成员）；ZbcWriter BuildType 尾部 enum 块（gated，非 enum 类字节不变）+ InternPoolStrings 预扫成员名；zbc 1.21→1.22 / zpkg 0.25→0.26。**踩坑**：enum IrClassDesc 初用短名 `ed.Name`（普通类用 `_q()` FQ）→ typeof(Enum) 按 FQ 查不到 → IsEnum=false；改 `_q(ed.Name)` 根治。两代自举（0.25 老种子 release VM 快跑）+ gen3 修复重建 + 不动点 7/7；GREEN：xtask test 单跑（e2e 197/197 含 enum_reflect + stdlib 含 reflection.z42 enum [Test] + compiler 7/7 + vscode）+ cargo metadata 177/zbc_compat 3/lazy_loader 16/loader 48。typeof(enum) 编译期已由 ResolveTypeP EnumTypes→Z42ClassType 支持，无需 SymbolCollector 改。TSIG enum 块 P1 不删（P3 删）。| **~~reencode-strs-segment-dict~~** ✅ 已归档 2026-07-09（STRS 字符串池重编码为 segment-dict：段字典去重 + 名字=段索引序列 join('.')，吞并 varint-长度（删冗余 offset）+ 前缀去重两目标，−44% STRS/≈−10% 整包；zbc 1.20→1.21 + zpkg 0.24→0.25。ByteWriter.WriteVarint + ZbcWriter.BuildStrs/_splitDots 重写 + ZbcReader.DecodeStrs + ZpkgReader.Open segment-dict + 两游标 Varint。**本地两代自举 bump 路径首次真实跑通**（旧种子 gen1→gen1-stdlib→gen2 0.25→新 VM），不动点 gen2==gen3 7/7 byte-identical。**实施踩坑根治**：ZpkgReader.Open 内层 STRS 循环误用 `name` 与外层 META 包名冲突（z42 局部变量非块作用域 shadow）→ z.Name 被污染 → _isPrelude 恒 false → prelude 包不激活 → 所有 no-using/bare 名解析回 null → 136 e2e 全红；改名 poolStr 根治，规则入 compiler-z42c.md。GREEN：xtask test 单跑全绿（e2e 196/196 + cross-zpkg + stdlib + compiler 7/7 + vscode-syntax）+ cargo metadata 177/zbc_compat 3/lazy_loader 16/loader 48；regen zbc-format 6 + zpkg-format 4 + 2 golden hex + zpkg header pin + Rust 2 pinned + loader_tests helper）。历史：**~~add-indexed-zpkg-min-patch~~** ✅ 已归档 2026-07-09（indexed zpkg 重设计→最小 patch：主文件 FILE 段（ns/src_rel/src_hash/fnCount/firstSig/zbc_hash）+ 自包含散装 fullMode zbc（cache 原样投影，未变文件字节+mtime 不动）；`pack` 决议接通（[project].pack + 内置默认 debug→indexed/release→packed；`pack=false ∧ --release` 报错）；`ZpkgIndexedWriter.WriteIndexedMain` + `ZpkgReader.Open` 放行 + `ReadModuleSigs` FILE 分支；VM `load_zpkg_indexed`（散装 zbc 相对主文件目录装载 + plain BLAKE3-128 内容 hash 校验 + `assemble_zpkg_artifact` packed/indexed 零分叉）；zpkg minor 0.23→0.24（packed 布局字节零变化）。driver 拆 `IndexedDist.z42`/`BuildPaths.z42`、project 拆 `ZpkgWriterIndexed.z42`（500 行硬限）。GATE4 一次干净全绿（e2e + stdlib 272/272 + compiler 19/19 + 不动点 7/7 + vscode-syntax）+ 对账器 dist 全文件字节一致（demo 5/5 + xtask 16/16）+ Rust indexed 单测 3/3 + 4 fixture regen；实施坑：debug/release 双 VM 都要 rebuild（regen 波用 debug VM）、golden header 断言 23→24、build_id::compute 尾清零语义不可混用。**队列**：`add-partial-types`（🔴 DRAFT 已起草 2026-07-08，compiler 单锁——partial 类型编译期合并、零格式 bump；等本 change 释放 compiler 锁后进阶段 6.5）。历史：**~~add-file-level-incremental~~** ✅ 已归档 2026-07-08（文件级增量编译：cache 升 SoT（`<rel>.zbc` fullMode + `<rel>.meta` v2（hash/ns/usedDepNs/token 集/D5a 残留 pool·labels/三重版本 pin）+ 包级源清单）→ dist 为 cache 确定性投影（D7 IrModule 重序列化、组装零分叉）；种子 + token 保守边传递闭包 → 仅失效闭包重编，其余 ZbcReader 读回 + meta 残留回填；ZbcReader/ZbcReaderInstr 移植（wire 全段 + REGT 回填 + bit31/u32 哨兵坑）。**D8 性能三轮修正**（首轮对账器逮出 worst-case 慢 17% → token 入 meta v2 + `Parser.Tokens()` 一行访问器（Scope 追认）+ SHA-256 3 遍→1 遍）→ 终态 worst-case +1.9% 持平、全量提速 8s、no-touch 跳过 9×。硬验收：暴力对账器 `test incremental` 两轮全扫 **47/47 byte-identical**（demo 多特性 + xtask 42 文件）+ 全 stage GREEN + 自举不动点。**队列**：`add-indexed-zpkg-min-patch`（方向草案，起草全量 spec 过 6.5 后占 compiler+runtime 双锁）。历史：**~~add-vscode-syntax-ext~~** ✅ 已归档 2026-07-07（compiler 部分：z42c `--dump-keywords` 导出动词——Lexer 只读访问器 `KeywordCount`/`KeywordNameAt` + `DumpTool.DumpKeywords`（87 关键字注册序）+ driver 无 file 参数动词分发 + lexer 单测 ×2；自举不动点 7/7 不回归。toolchain 部分见 toolchain 行）。**~~port-incremental-build-cache~~** ✅ 已归档 2026-07-05（z42c 单工程增量编译：单文件 fullMode `.zbc` cache 落盘（`cache_dir` → `${output_dir}/.cache` → `<projDir>/.cache` 级联）+ 整包 probe（hash/cache zbc/TSIG 三校验；采 C# 最终形态 ed901f01——any-fresh→整包失效、全命中→完全跳过重写 + 超 C# 的记录数防删洞；混合重建/ZbcReader 按裁决不移植，project.md 增量节文档漂移一并修正）+ `--no-incremental`/`Z42_INCR_DEBUG`；ZpkgReader 补 ReadSourceHashes；workspace/flat 不落 cache（gen 门禁零扰动，布线 Deferred `incremental-future-workspace-wiring`）。零格式 bump、VM 零改动。GREEN 全 stage + 不动点 7/7 + probe 单测 6/6 + launcher e2e 五场景（touch 重建与全量逐字节相等））。历史：**~~simplify-compiler-build~~** ✅ 已归档 2026-07-05（z42c build(exe) 自包含复制非 stdlib 兄弟包 + 产物目录 z42c→compiler 镜像源码 + env 折叠 Z42_HOME（删 Z42C_DIR/Z42_TOOLCHAIN + 布局守卫）；删 selfbuild-runlibs/dogfood scratch 目录。回归修复：自举不动点 gen2 改走 --workspace（Phase 2 切 --workspace 后 gen2 仍走胖-flat 逐包 → 非确定漂移 → CI 红）+ _copyAll self-copy 守卫。CI b0ba6ce2 全绿 34/34）。**~~add-params-varargs~~** ✅ 已归档 2026-07-01（`params` 变长参数：typed `T[]` + `object[]` 混类型重载，normal/expanded 双调用形态。阶段 1-3 随 `add-type-based-overloads` 附带落地（token/parser/`Param.IsParams`/`Z42FuncType.ParamsFrom`/`SymbolCollector`，e924236c，inert）；本变更续阶段 4 起：TypeChecker `_resolveParamsOverload`/`_withParamsExpansion` 重载决议 + 跨包 TSIG（`ExportedFuncZ`/`ExportedMethodZ.ParamsFrom`，zpkg minor 0.22→0.23，`ZpkgWriter`/`ZpkgReader` strict-pin + `ImportedSymbolLoader` 回填）。纯编译器前端：VM 零改动、零新 IR 指令、零 zbc 格式 bump（仅 zpkg TSIG 追加 1 字节）。GREEN：VM golden 193/193（含 3 个新增 params golden，真实执行验证，非降级）+ z42c 单元 30/30；z42c 全量自举 + cross-zpkg 端到端受 pre-existing、正交的 [self-hosting-future-single-vm-bootstrap-gap](../../design/compiler/self-hosting.md#self-hosting-future-single-vm-bootstrap-gap) + [self-hosting-future-attributesynth-cross-pkg-resolution](../../design/compiler/self-hosting.md#self-hosting-future-attributesynth-cross-pkg-resolution) 阻断，降级验证（用户已接受，与本变更逻辑正确性正交）。stdlib API 迁移到 `params`（Path.Join/String.Join/Concat/Format）延后为独立 follow-up change，晚一个 nightly 发布，避免破坏 z42c 自消费 API 的自举不动点）。历史：**~~add-type-based-overloads~~** ✅ 已归档 2026-07-01（type-based 方法重载决议：z42c 从 arity-only（`Name$arity` 同 arity 冲突覆盖）→ 按实参类型决议（适用性+最具体+歧义）+ 同 arity 用 type-mangled 键；静态方法先行落地，随后扩展到实例方法（协议豁免名单 `ToString`/`Equals`/`GetHashCode`/`GetType`/`get_Item`/`set_Item`）。纯编译器前端：VM 零改动、零新 IR opcode、零 zbc/zpkg 格式 bump。阶段 8 GREEN gate 暴露并修复 virtual/override 同 arity 重载 vtable slot 错位 bug（`SymbolCollector._passFixupOverrides`）。全 GREEN：vm/cross-zpkg/stdlib/compiler + self-host 7/7 byte-identical。解锁 `add-params-varargs` 阶段 4）。**~~fix-golden-harness-interp-pin~~** ✅ 已归档 2026-06-21（C# GoldenTests harness 固定 `--mode interp`：jit-default 翻转后参考门静默改跑 jit → `gc_oom_exception`（interp_only）macos-15 trap；恢复 interp 参考契约，jit 覆盖归 xtask VM 门；dotnet 1571/1571）。**~~restructure-publish-output-dirs~~** ✅ 已归档 2026-06-19（publish_dir 统一；output_dir 默认改 artifacts/${project_name}/${profile}；exe 自动 publish 依赖复制；z42c publish 命令）|
| `runtime` | `—` | — | **空闲**。**~~add-nested-types~~ ✅ 已归档 2026-07-25（#38 合并，释放 runtime 锁）**：嵌套类型反射 builtin（`__type_nested_types`/`__type_declaring_type`/`__type_is_nested` + `GetMembers` 追加嵌套类型 + `Type.Name` split `.`/`+`；无 format bump）。**make-vm-loading-lazy 阶段2「后续」未开工**——历史（make-vm-loading-lazy）：🟡 vm（阶段 1：统一 jit/interp 惰性加载路径——JIT 删 eager 全量 BFS 合并，跨包 Call 经 `resolve_id_by_name`→`try_lookup_function`→`compile_fn` 走 native + 每调用点 `call_jit_ic`（AtomicU32 缓存 lazy/merged id，稳态无回归）；static-init 二阶段镜像 interp（eager sorted + lazy force-load）；ConstStr 溢出走 `try_lookup_string`。仅 AOT 保留 eager。无格式改动，本地 835 lib 单测 + 4 新 lazy_load 单测全绿、release 零告警；e2e/golden 逐字节 gate 以 CI 为权威。阶段 2（FIDX/TIDX 格式 bump + 类型按需 parse）后续，届时占 `runtime`+`ir` 双锁）。历史：**~~lazy-per-function-jit~~** ✅ 已归档 2026-07-23（JIT 从 eager 全量编译改为惰性逐函数 compile-on-first-call；根治 main CI `test-vm-jit` 超时——每 golden 用例冷编整套 stdlib；PR #18 合并。本 change 在其基础上进一步删 eager 合并、统一 interp 路径）。历史：**~~drop-tsig-expt~~** ✅ 已归档 2026-07-11（P3：ZPKG_VERSION_MINOR 30→31；SEC_EXPT 无消费方保留常量。详见 compiler 行）。历史：**~~add-delegate-metadata~~** ✅ 已归档 2026-07-11（P1-e ②：CLASS_FLAG_DELEGATE=1<<6 + TypeDesc.is_delegate + `__type_is_delegate` builtin；常量 26/30。详见 compiler 行）。历史：**~~add-crosspkg-impl-reflection~~** ✅ 已归档 2026-07-11（unify **P1-e ①**：VM 读现有 IMPL 段（`read_zpkg_impl_pairs`——只取 (target_fq,trait_fq) 对，方法记录 17+pc×8 跳过）→ `LoadedArtifact.impl_pairs` → `LazyLoader.impls` 注册表（lazy 合并 + eager `seed_lazy_loader_impls`）→ `builtin_type_interfaces` base 链每类并入 impl traits（同 BFS 传递闭包/去重）。**无格式 bump、z42c 零改动**（IMPL 重定性为统一元数据：z42c+VM 都读，D2）。加载语义：反射只见已加载包的 impl。cross-zpkg e2e `impl_reflect`（GetInterfaces 含 IGreet + 派发不回归）4/4。GREEN 全绿 + 不动点 7/7 + cargo lib 782+21。）历史：**~~add-param-metadata~~** ✅ 已归档 2026-07-10（P1-d：`FuncSig`/`Function.min_arg/params_from` + `FunctionCold.param_names/param_defaults`；read_sigs 交错读每参 name+default；`build_method_info` ParameterInfo 塞 IsOptional/IsParams/DefaultValue + Name SIGS 优先；常量 25/29。详见 compiler 行）。历史：**~~add-method-modifiers~~** ✅ 已归档 2026-07-10（P1-c：`FuncSig`/`Function.method_flags` + read_sigs + 灌入；`build_method_info` IsVirtual 权威化（`sig_found?flag:vtable`）+ IsAbstract；`ZBC_VERSION_MINOR` 23→24 / `ZPKG` 27→28。详见 compiler 行）。历史：**~~add-member-visibility~~** ✅ 已归档 2026-07-10（P1-b：`FieldDesc.visibility`/`FuncSig.visibility`/`FieldSlot.visibility`/`Function.visibility`；`read_type` 两字段块 + `read_sigs` 读 visibility；loader/dispatch/resolver 线程；`build_field_info`/`build_method_info`+`resolve_func_sig` 反射塞 IsPublic/IsPrivate；`ZBC_VERSION_MINOR` 22→23 / `ZPKG_VERSION_MINOR` 26→27。详见 compiler 行）。历史：**~~add-enum-type-metadata~~** ✅ 已归档 2026-07-09（P1-a：`CLASS_FLAG_ENUM=bit5`；`read_type` gated 读 enum 成员块 → `ClassDesc.enum_members`；`TypeDesc.enum_members`+`is_enum()`（class_flags 派生）；loader 组装；反射 builtin `__type_is_enum`/`__enum_names`/`__enum_values`/`__enum_name`+注册；`ZBC_VERSION_MINOR` 21→22 / `ZPKG_VERSION_MINOR` 25→26。详见 compiler 行）。| **~~reencode-strs-segment-dict~~** ✅ 已归档 2026-07-09（VM `read_strs` 改读 segment-dict（段字典 + `Cursor::read_varint` + 名字重组 join('.')）+ `ZBC_VERSION_MINOR` 20→21 / `ZPKG_VERSION_MINOR` 24→25 + changelog；loader_tests `make_fake_zpkg` STRS 改 segment-dict + 2 pinned-version 断言。详见 compiler 行）。历史：**~~add-indexed-zpkg-min-patch~~** ✅ 已归档 2026-07-09（VM indexed 装载 `load_zpkg_indexed` + `read_zpkg_file_entries` + zbc 内容 hash 校验 + `ZPKG_VERSION_MINOR` 0.23→0.24；详见 compiler 行）。历史：**~~fix-signal-test-stale-version~~** ✅ 已归档 2026-07-07（`signal_handler_e2e.rs:76` build-banner 断言硬编码 `z42vm 0.1.0`，crate 已 bump 0.3.0 → 全 7 个 `#[cfg(unix)]` 信号测试在 CI unix 全腿放开后首次真跑即红。改用 `env!("CARGO_PKG_VERSION")` 跟随版本。与 fix-process-run-timeout-orphan 同源=全腿放开新暴露的 pre-existing bug。CI stderr dump 证版本断言为唯一失败点（call-stack/VmCore 标记在场）；本地信号沙箱 hang 故 GREEN 以 CI 为准，`cargo test --no-run` 编译通过）。**~~fix-process-run-timeout-orphan~~** ✅ 已归档 2026-07-07（`builtin_process_run` 超时只 kill 直接子进程、不 kill 进程组 → `sh -c "sleep 5"` 的 grandchild 孤儿持有继承的 stdout/stderr 管道写端 → reader 线程 join 阻塞到孙进程自然退出（超时 result=KIND_TIMEOUT 正确但函数迟 ~5s 返回）。修：unix 下 `process_group(0)` 生子进程组 + 超时 `kill_process_tree`（`kill(-pid, SIGKILL)` 灭整组 + `child.kill()` 兜底）。本地 `run_timeout_fires_for_long_running_child` 0.20s 通过（原 5.0018s）+ gate 无回归。**解锁 redesign-xtask-test 的 `test runtime` 全腿覆盖**（该 unix-only 测试历来只在跳过它的 Windows 腿"跑"，全腿放开首次真跑即暴露此 pre-existing bug）。历史：**~~simplify-compiler-build~~** ✅ 已归档 2026-07-05（build.rs 回归修复：dogfood/run-release → 自包含 driver `compiler/z42c.driver/dist` + `Z42_LIBS=libraries/dist`，恢复 C7/embedding e2e fixture；见 compiler 行）。**~~fix-jit-cross-zpkg-call~~** ✅ 已归档 2026-06-20（JIT eager 加载改 transitive BFS → 传递依赖如 `Std.Toml.TomlValue.Parse` 在 `--mode jit` 正确解析；+ `jit_call` interp 兜底防御网；z42c 现可 `--mode jit` 运行，byte-identical，z42.core 编译 16.9s→7.9s）。**~~runtime-dynamic-load-call~~** ✅ 已归档 2026-06-19（接口落地；VM builtin 延后——见 launcher-command-dispatch.md Deferred workload-future-b2-packaging）。（前序 ~~add-reflection-transitive-interfaces~~ ✅ 2026-06-17）|
| `stdlib` | `converge-z42c-onto-z42-project` | 2026-07-11 | 🟡 阶段 0 spike + z42.project 登记 member + 首个 GREEN。**~~fix-repl-sdk-layout~~** ✅ 已归档 2026-07-26（PR #39 合并、短占 stdlib 锁已释放；User 授权预抢——隔离 worktree `z42-replfix-wt`；修 `z42.io.Path.GetDirectoryName/GetFileName` 只认 `/` 不认 `\` 的 Windows bug——`Z42_PORTABLE_VM=C:\…\z42vm.exe` 令 launcher `_home()` 目录解析失败、退到 `.z42` 相对兜底 → `z42 repl` 误报「programs/interactive/ 缺失」（文件其实在）。加私有 `_isSep(c)={'/','\\'}`；`/`-only 输入行为不变 → 自举字节中性。同 PR 附 toolchain：launcher `_forwardRepl` 改 `Z42_LIBS` 单目录（`Z42_LIBS` 是单 PathBuf，原 `:` 拼多段在 Windows 撞盘符 `C:\`）。z42i shipped 求值报 `undefined Lexer.Tokenize` 的**分层欠债**（z42.scripting 在 libs/ 却传递依赖不在 libs/ 的 z42c.\*）归 `converge-z42c-onto-z42-project`（抽 z42c 基础能力入标准库），见 add-z42-repl 遗留、不打 colocation 补丁。GREEN：Path Windows/POSIX 单测过、z42.io 自建过；完整 gate 以 CI 为权威。归档即还锁）。**~~perf-dictionary-hashes~~** ✅ 已归档 2026-07-21（短占 stdlib 锁——perf change #4：`Dictionary`（开放寻址表，prelude）加 `int[] hashes` 平行数组，探测先比存储 hash 再 `.Equals()`（int 不等即短路，跳过绝大多数 string Equals O(len) 派发）+ capacity 恒 2 的幂故 `& (cap-1)` 掩码取代 `% cap` + Grow 复用存储 hash 免重算/免 Equals。行为保持（探测语义、迭代顺序、扩容阈值不变）。**dict_set_get 1.030→0.881ms=1.17×**。D2（TryGetValue）不可行（z42 无 out 参数）。GREEN：z42.core 8 测试全绿。归档即还锁）。**~~perf-string-format~~** ✅ 已归档 2026-07-21（短占 stdlib 锁——perf change #3：String.Format 链式 Replace→单趟扫描替换（args 一次 stringify + 两趟算长/填 char[]）。行为保持（非递归，C# 语义）。**string_format 2.07→0.896ms=2.31×**。course correction：同批尝试 Join/Concat char[] 改写实测慢 4.5×（小 n 下 `+` StrConcatInstr 优于 CharAt builtin 分发）→ 回退保原 `+`。GREEN：z42.core 8 测试全绿。归档即还锁）。**~~perf-regex-backtrack-alloc~~** ✅ 已归档 2026-07-21（短占 stdlib 锁——perf change #2：Regex 回溯引擎R1 量词回溯用 positions[i] 取代 O(k²) 重跑子匹配（无捕获组）+ R2 ResetState 复用 group-slot 数组（匹配取自有拷贝）+ R3 无捕获组时 Snapshot/Restore 返 null。行为保持。收益 **regex_is_match 6.97→3.90ms=1.79×**、find_all 1.23×。GREEN：regex 13 测试文件全绿。归档即还锁）。**~~perf-crypto-bigint-hotpaths~~** ✅ 已归档 2026-07-21（短占 stdlib 锁，User 授权预抢——perf 攻坚 change #1：SHA256 `_roundConstants`→static `_K`、BLAKE3 `_sigma`→static `_SIGMA` + `_compress` 内联 iv[0..3]、BigInt.Parse 改 9 位分块。行为保持（哈希逐字节一致）。收益：**bigint_parse 2.96→0.41ms=7.27×**、blake3_4k ~6%、**sha256_4k 噪声（无收益——证实瓶颈是 runtime 数组访问锁 F1，非常量重建）**。GREEN：crypto 28 + numerics 16 测试全绿。归档即还锁）。**~~rebuild-bench-structured-output~~** ✅ 已归档 2026-07-19（短占 stdlib+toolchain 锁，User 授权预抢——隔离 worktree `z42-bench` off main；重建 bench 结构化输出：z42.test 新增 `BenchStats.parse`（解析 benchmark 摘要行）+ `TestReport`（`resultJson`/`esc` 手写 JSON 助手）+ `Runner` json 模式（`TestIO.captureStdout` 捕获 benchmark stdout → `BenchStats` → 内联装配 `{tool,module,summary,results}` 报告）+ z42b `test/bench --format {pretty|json}`；修正 Bencher/README 指向已删 exec.rs 的 stale 契约。**实施撞两个 nightly-z42c 前端 bug**（数组类型参数误解析为 `new` / 多参 brace-body 静态方法 TSIG 丢导出，探针 A–S 定位），规避：TestReport 只留 1-arg 助手、envelope 由 Runner 内联装配（compiler 锁被占，前端修复记 Deferred `bench-structured-future-report-envelope`）。GREEN：z42.test 本地全绿（9 bench 单测）+ `z42b bench --format json` 端到端产真实 bench_stats；完整 origin/main gate 以 CI 为权威。commit 69bc9cf2。附带 z42.core bench 套件（独立 commit 929922b8，test 类型）。归档即归还 converge）。**~~dedup-crypto-ct-compare~~** ✅ 已归档 2026-07-16（review §2.3 follow-up：4 处确认整数组等长的手写常数时间比较（ChaCha20Poly1305/AES-GCM/AES-CCM tag + RSA EMSA-PKCS1）收敛到公开 `ConstantTime.Equals`；保留 2 处非等长（RSA PSS/OAEP 前缀）不动。纯内部收敛行为不变，crypto 28 文件向量测试全过。隔离 worktree full GREEN）。**~~add-crypto-constant-time-equals~~** ✅ 已归档 2026-07-12（review §2.3：AEAD 内部手写常数时间 tag 比较无对外 API，用户校验 MAC/token 只能用 `==`（时序泄漏）；新增 `Std.Crypto.ConstantTime.Equals(byte[],byte[])`（长度不等即 false 对齐 .NET FixedTimeEquals，等长无短路遍历累积差异）+ 6 回归测试 + crypto README。隔离 worktree full GREEN。短占 stdlib 锁，归档即归还 converge）。**~~fix-memorystream-seek-overflow~~** ✅ 已归档 2026-07-12（review §2.1：`MemoryStream.Seek` 把 long newPos 直接 `(int)` 存入 _position，超 int.MaxValue 静默回绕损坏（负检查挡不住超大正值）；加 `newPos>2147483647L` 越界抛 ArgumentException + 回归测试。隔离 worktree（0.32 工具链，镜像自主树 + cargo 0.32 debug/release VM）full GREEN。短占 stdlib 锁，归档即归还 converge）。**~~add-linkedlist-stream-byte-api~~** ✅ 已归档 2026-07-13（review §2.6：`LinkedList.ToArray`（ToArray 家族收尾，head→tail 非破坏快照）+ `Stream.ReadByte`（0..255/-1）/`WriteByte`（默认基于 Read/Write，子类可覆写）+ 回归测试。隔离 worktree full GREEN）。**~~add-collection-toarray~~** ✅ 已归档 2026-07-12（review §2.6：Queue/Stack 无 ToArray（不破坏结构无法枚举）、List 缺 AddRange/ToArray；补 `Queue.ToArray`（FIFO ring 快照）/`Stack.ToArray`（top-first 对齐 C#）/`List.ToArray`+`List.AddRange`——均非破坏性 + 回归测试。隔离 worktree full GREEN）。**~~add-core-char-string-api~~** ✅ 已归档 2026-07-12（review §2.6：z42.core 补 `Char.IsDigit/IsLetter`（ASCII 纯脚本，替调用方手写 `(int)ch<48||>57`）+ `String.ToCharArray`（FromChars 的逆，O(1) 下标）+ 回归测试。`Dictionary.TryGetValue` 需 out 参数 z42 无 → 跳过。隔离 worktree full GREEN）。**~~add-math-sign-clamp~~** ✅ 已归档 2026-07-12（review §2.6：`Math` 补 `Sign`/`SignInt`/`Clamp`/`ClampInt` 纯脚本（int/double 分名，沿用 Abs/AbsInt）+ README 索引 + 回归测试。隔离 worktree full GREEN）。**~~fix-double-compareto-nan~~** ✅ 已归档 2026-07-12（review §2.2：`Double.CompareTo` 朴素 </> 对 NaN 返 0 破坏全序 → List<double>.Sort 未定义；改镜像 C# 语义 NaN 排最前+等于自身（`x!=x` 检测）+ 回归测试（NaN 经 double.Parse("nan") 构造）。隔离 worktree full GREEN）。**~~fix-toml-number-scan~~** ✅ 已归档 2026-07-12（review §2.2：`TomlParser.ParseNumber` 扫描过宽——放行 `1.2.3`/`1e5e5`（double.Parse 裸抛）、前导零 `042`（long.Parse 静默接受）；修：最终 parse 入 try/catch → `Err`（带行列 TomlException）+ `RejectLeadingZeros`（0/0.5/0e0/-0 仍合法）+ 回归测试。隔离 worktree full GREEN。短占 stdlib 锁，归档即归还 converge）。**~~fix-parser-depth-limit~~** ✅ 已归档 2026-07-12（review §2.2/item 8：`JsonParser`/`TomlParser` 的 `ParseValue` 递归无深度上限，深嵌套输入打爆 VM 栈；两 parser 加 `_depth` 计数器（入口 +1 查 >256 抛 JsonException/TomlException，try/finally 各返回路径 -1）+ 回归测试（300 层抛异常 / 100·50 层合法解析可下钻）。**YAML 递归复杂留独立 follow-up**。隔离 worktree 实施+full GREEN 全绿。短占 stdlib 锁，归档即归还 converge）。**~~fix-iso8601-day-of-month~~** ✅ 已归档 2026-07-12（review §2.2：`DateTime.ParseIso8601` 只查日 1..31，`"2026-02-31"` 静默滚成 3/3，而同文件 `DateTime.Utc` 已按 `DaysInMonth` 严格校验；改 ParseIso8601 镜像之 + 回归测试（拒 02-31/04-31/非闰 02-29，接受闰年 02-29 + 各月末）。**在隔离 git worktree 内实施+验证**——主树被并发会话未提交的 params 迁移（Path.Join/String.Join）污染无法构建，worktree 以 clean HEAD + clean 0.31 种子隔离（"并行推进"，无 clobber，full GREEN 全绿）。**短占 stdlib 锁**，归档即归还 converge）。**~~fix-json-bigint-overflow-fallback~~** ✅ 已归档 2026-07-12（review §2.2：`JsonParser` 整数 `long.Parse` 无 try/catch——超 i64 合法 JSON 整数溢出裸抛（注释自称回退 f64 却无 catch）；补 try/catch → `OfDouble`（lossy，对齐 serde_json）+ 回归测试（10^20 回退 double / i64 max 仍 long）。**短占 stdlib 锁**，归档即归还 converge。full GREEN 全绿）。**~~fix-crash-cluster~~** ✅ 已归档 2026-07-12（review §2.1 排名 #3 崩溃/状态损坏簇，3 独立 commit：①`Dictionary.FindSlot` 哈希取负溢出（int.MinValue）→ `& 0x7FFFFFFF`；②`BigInt.Equals(object)` 无类型检查强转 → `is BigInt` 模式；③`String.Substring` 越界/负 length trap → 边界检查抛 Exception。各带回归测试。**Assert.Equal(null) 空安全放弃**——反复 FieldGet-on-Null 崩，根因在 VM/编译器层（object? 空值经两参静态 Equal 派发），非 stdlib 空检查能修，按根因/设计完整性停下、已回退、留 VM 层独立调查。**短占 stdlib 锁**（User Continuity 授权，归档即归还 converge）。full GREEN 全绿（并发 clobber 后等 4-streak 安静窗口））。**~~fix-queue-stack-empty-guard~~** ✅ 已归档 2026-07-12（review §2.1 排名 #2：`Queue<T>.{Dequeue,Peek}`/`Stack<T>.{Pop,Peek}` 空容器无检查→读陈旧槽 + count 减到 -1 静默状态损坏；加空检查抛 `Exception` 对齐同包 PriorityQueue/LinkedList + 6 回归测试（空抛异常 + count 不变负 + drain-then-empty）。**短占 stdlib 锁**（User Continuity 授权续做批次 A 高危 fix，归档即归还 converge）。full GREEN 全绿）。**~~fix-sync-lock-release-on-throw~~** ✅ 已归档 2026-07-12（review §2.4 排名 #1：Mutex/RwLock 回调抛异常→`__*_unlock`/`__*_release` 被跳过→锁永不释放死锁；`Lock`/`Read`/`Write`/`TryRead`/`TryWrite` 加 try/finally 保证 release + 校正 Mutex.z42 类头假注释 + 回归测试 `lock_release_on_throw.z42` 5 例（Mutex/RwLock Read/Write/TryRead/TryWrite 抛异常后再取锁不死锁 + 值未写入）。**短占 stdlib 锁**（User 授权高危单项 fix 先于 converge，归档即归还）。full GREEN 全绿（e2e 197/0 + cross-zpkg + stdlib 全库 0 failed + 自举不动点 7/7 + vscode）。附带修复：前序会话遗留的半 bump `.z42` 本地种子（0.29 vs 0.31 源）用 in-tree 0.31 产物刷新恢复）。历史：**~~add-delegate-metadata~~** ✅ 已归档 2026-07-11（P1-e ②：Type.z42 `IsDelegate` getter + reflection.z42 test_delegate_metadata [Test]。详见 compiler 行）。历史：**~~add-param-metadata~~** ✅ 已归档 2026-07-10（P1-d：`ParameterInfo.z42` 加 IsOptional/IsParams/DefaultValue；reflection.z42 3 个 param_metadata [Test]。详见 compiler 行）。历史：**~~add-method-modifiers~~** ✅ 已归档 2026-07-10（P1-c：`MethodInfo.z42` 加 `IsAbstract`（`IsVirtual` 已存）；reflection.z42 test_method_modifiers + test_override_is_virtual_not_abstract [Test]。详见 compiler 行）。历史：**~~add-member-visibility~~** ✅ 已归档 2026-07-10（P1-b：z42.core `Reflection/FieldInfo.z42` + `MethodInfo.z42` 加 `IsPublic`/`IsPrivate`（protected → 两者皆 false，镜像 C# IsFamily）；reflection.z42 test_field_visibility + test_method_visibility [Test]。详见 compiler 行）。历史：**~~add-enum-type-metadata~~** ✅ 已归档 2026-07-09（P1-a：z42.core `Type.IsEnum` + 新 `Std.Enum`（`GetNames`→string[] / `GetValues`→long[]（底层 i64 不截断）/ `GetName`）。详见 compiler 行）。| **~~fix-blake3-multichunk-root-flag~~** ✅ 已归档 2026-06-22（z42.crypto Blake3 多块（>1024B 树）哈希错误：中间 parent 误标 ROOT + CV 取值二次 feed-forward；修 `_parentCV`/`_chunkCV`。影响所有 >1024B BLAKE3；小向量测不到。修后 z42c build_id == C# nuget 逐字节一致 + 回归测试。strip-symbols 暴露）。**~~fix-flaky-periodic-timer-test~~** ✅ 已归档 2026-06-21（z42.threading 周期定时器 test 固定 500ms sleep → 轮询等待 fires>=3（cap 5s）；macos-15 CI 时序 flaky 根治；本地 12/12）。**~~runtime-dynamic-load-call~~** stdlib 部分 ✅ 2026-06-19（`Std.Runtime` extern 接口已落 z42.core/src/Runtime.z42）。（前序 ~~add-reflection-assignable-from~~ ✅ 2026-06-16）|
| `compiler`（归档） | — | — | 原 `z42c` 子系统已并入 `compiler`（两者一直同指 `src/compiler/`）；以下为其归档记录：**~~port-z42c-impl-block~~** ✅ 已归档 2026-06-23（z42c 端口 `impl Trait for Type` 跨 zpkg：parser ImplDecl + 同包 binder + IMPL 段 5 组件（结构 byte-identical）+ 消费端 _readImpl/_mergeImpl。端到端 cross-zpkg/impl_propagation z42c 建+跑 → "hi from R2"，vcall_base_fallback 亦通过 → **两 cross-zpkg fixture z42c 全绿**；C# 测试 2/2 不回归。fixpoint gen1==gen2 绿。整包 byte-identical 暂不追求（非关键路径，IMPL emit 已结构一致；C# re-export imported trait 接口差异记 Deferred）。fixture 补 `using Std.IO`（strict-using 合规）。= replace-csharp B 前置满足）。**~~fix-z42c-ctor-this-delegation~~** ✅ 已归档 2026-06-22（z42c 把构造器 `: this(args)` 委托误编为 `: base(args)`——`Parser._parseMethodTail` 消费 this/base 关键字后丢弃区分，`TypeChecker._bindMethodBody` 一律解析基类 ctor。`Bencher() : this(10,100)` → 调不存在的 Object 2-arg ctor → 字段保持默认 → warmup+samples=0 → `test_bencher_default_runs_warmup_plus_samples` `expected 110 but got 0`。修：`MethodDecl` 加 `IsThisInit` 位 → parser 记录 → TypeChecker 按位选 TargetCls（this→curCls，base→baseCls）。镜像 C# `Ast.BaseCtorArgs`/`ThisCtorArgs`。回归 `codegen_tests.test_ctor_this_delegation`；S3 full gate 全绿收官第 8 个 bug）。**~~fix-z42c-static-field-assign~~** ✅ 已归档 2026-06-22（z42c `_emitAssign` 无 `BoundStaticGet` LHS 分支 → `Class.staticField = v` 静默丢弃 → 静态字段 mutation 不持久。修：加 StaticSetInstr 分支。DiagnosticsFilter 4/4 + compiler-z42 7/7。附：blake3 多块「codegen bug」实为测试 golden 误写，已改正——无 codegen bug）。**~~fix-z42c-static-call-cross-ns~~** ✅ 已归档 2026-06-22（z42c 静态调用 OwnerClass 限定用 `Qualify`（当前 ns）而非 `QualifyClass`（imported aware）→ 同包跨-ns 静态调用 Zip→Deflate undefined。修 ExprEmitter 用 QualifyClass；zip-deflate OK + compiler-z42 7/7 不回归。S3 codegen bug #1/3）。**~~fix-z42c-native-named-entry~~** ✅ 已归档 2026-06-22（z42c `_nativeIntrinsic` 只读 positional `[Native("__name")]`，对 named 短形 `[Native(lib=,entry=)]`（z42.compression 8 文件）返 "" → 不发 builtin 桩 → `_CompressRaw` 等 undefined。修：named args 取 entry 值发 BuiltinInstr。compression 18→8 + compiler-z42 7/7 不回归）。**~~port-z42c-strip-symbols~~** ✅ 已归档 2026-06-22（z42c 发行构建端口 strip-symbols：MODS dbug_len=0 + BLID（BLAKE3-128）+ `.zsym` sidecar；z42c.driver FULLY byte-identical to C# 含 BLID + StdlibSidecarPairingTests 过 + compiler-z42 7/7）。**~~fix-z42c-generic-ctor-arity~~** ✅ 已归档 2026-06-22（z42c `new C<T>()` 对 arity-overloaded 同名类误取 arity-0 base → throw 错类型 catch 失配；修 `SymbolTable.ResolveTypeP` 泛型实例化优先 `Name$N`。S3 dogfood multicast 暴露；2 aggregate golden MATCH + compiler-z42 7/7 不回归）。**~~fix-z42c-tsig-optional-params~~** ✅ 已归档 2026-06-21（z42c TSIG 把可选参数 `Param.Default` 当必填 → minArgCount=paramCount，跨包省略默认实参报 E0402；修 `ExportedTypeExtractor._requiredCount`（镜像 C# BuildFuncSignature）。dogfood S3 暴露；compiler-z42 byte-identical 7/7 + C# 1571/1571 不回归）。**~~z42c-build-workspace~~** ✅ 已归档 2026-06-21（M1：`build --workspace --output-dir <flat>` 成员发现+拓扑+build loop+`clean`，byte-identical 7/7；**M2 per-member drop-in**：无 --output-dir 时按 `[workspace.build].output_dir` 模板展开 per-member 布局（PathTemplate `${project_name}` 别名补齐）+ `DepScan.ScanDirs` 多目录 first-wins 去重；22 库 per-member 布局 + 功能验证 OK + 拓扑单测 3/3。= replace-csharp-compiler **S3 能力前置**）。**~~port-z42c-self-compile~~** ✅ 已归档 2026-06-19（G22 7/7 全包 byte-identical 完成；2 stale [ ] 项由 G19a+G22 清零）。**~~scaffold-z42c-selfhost~~** ✅ 已归档 2026-06-19（B0 骨架 + port-z42c-* 系列全完成；30 子任务通过后续变更实现）。**~~port-z42c-syntax~~** ✅ 已归档 2026-06-19（z42c.syntax byte-identical；6e-2 dump 对账延后）。**~~port-z42c-project~~** ✅ 已归档 2026-06-19（z42c.project byte-identical；G18 覆盖）。（前序：~~mirror-build-output-per-component~~ ✅ 2026-06-16）|
| `toolchain` | `—` | — | **空闲**。**~~add-repl-decls-multiline~~ ✅ 已归档 2026-07-28**（REPL 多行 + 函数/类型声明累积 + 2 处 compiler 跨包类型元数据修复，见上方 prose；释放 toolchain 锁）。**~~add-z42-repl~~ ✅ 已归档 2026-07-27**（REPL MVP 发货，见上方 prose）。**~~add-stdlib-bench-baseline~~** ✅ 已归档 2026-07-20（隔离 worktree `z42-bench`；`bench stdlib --json <out>` 各库走 z42b `--format json` 聚合 bench_stats → 单个 schema-v1 baseline（`name="<lib>.<label>"`, `tier="z42-micro"`, `value=median_ns`, ci=min/max）；复用现成 `bench --diff` 做本地回归对比（零 diff 代码）；schema 加 `z42-micro` tier。capture 用 null-collector 守护共享 `test stdlib` 路径（零回归）；partial-failure 模块仍捕获通过项。**不进 CI 硬门禁**（micro ns 级共享-runner 噪声，README 既有结论）→ Deferred `stdlib-bench-baseline-future-ci-nightly`。验证：全库 46 benchmark/13 库、diff exit 0/1、pretty 不变。**发现 pre-existing 编译器 bug**：form-2 `[Benchmark] void f(Bencher b)` trampoline desugar 失效（`MethodInfo.Invoke expects 1 arg got 0`）→ 建议独立 change 修。完整 gate 以 CI 为权威。归档即还锁）。**~~xtask-analysis-round1~~** ✅ 已归档 2026-07-17（xtask 二次分析高价值安全项：删 2 处冗余 z42c 自建（test/bench stdlib prepare 波 + build all，_buildStdlib 已自建 z42c）+ bench-update.yml 缓存重定向 target dir（Swatinem host-v2）。compile-toolchain 绿）。历史：**~~consolidate-xtask-fns-round2~~** ✅ 已归档 2026-07-15（review §2 剩余安全项：§2.5 抽 `_runtimePkgScaffold` 收敛 ios/android/wasm + §2.10 三项小修（`_regenCore` 死参数 / bench ✔ 标签 / `_utcNow` 去重）。**含回归教训**：§2.5 误删 android `bool rel`（判未用漏 grep）→ compile-toolchain 全红 → 热修 `536bcb58` 恢复；compile-toolchain 绿验证。§2.4/§2.6 暂缓（warm 环境）、§2.7 不收敛（事实校正）、§5 process 落地 workflow.md。历史：**~~split-package-desktop-fn~~** ✅ 已归档 2026-07-15（review §2.8：拆 `_packageDesktop` 80→43 非注释行——[2c/5] 段抽 `_pkgStageToolchainComponents`（自足算 libs/hostVm/apphostStub/z42bZpkg）+ 8 连发 `_z42cBuildToml` 收敛 `buildTomls[8]`+while；5 组 `_z42bPublish` 保留显式。纯机械搬移，run 29422619619 `compile-toolchain`+`package-host`(4 OS) 全绿）。历史：**~~split-android-install-fn~~** ✅ 已归档 2026-07-15（review §2.8：拆 `_depsInstallAndroidSdk` ~161→49 行为 preamble + 6 逐步骤 helper（`_androidResolveJdk`/`_androidCmdlineTools`/`_androidSdkPackages`/`_androidNdk`/`_androidEmulatorExtras`/`_androidPrintExports`）；纯机械搬移，user-facing 字符串 sorted-diff 零差异 + 无变量泄漏；dev-only 路径 CI 只编译不执行、diff 核对为准。PR #8（762e3d41）合并 main、CI 全绿）。历史：**~~consolidate-xtask-fns~~** ✅ 已归档 2026-07-15（review §2.5+§2.7+§2.8：拆 `_testCrossZpkgImpl` ~164→53 行 + `_fixtureDist` 去重 7 处三层嵌套路径 + 上浮 ios/desktop 复制的 JUnit 骨架→`_writeJUnitReport`/`JUnitCase`/`_stdoutLines`/`_xmlEscape` + `_copyAbiHeaders` 收敛 6 处 File.Copy 对；纯机械搬移零行为变化，PR #6（dbcde969）合并 main、CI 全绿）。历史：**~~consolidate-z42c-invocations~~** ✅ 已归档 2026-07-12（§2.1 收敛 z42c 调用面进 `_z42cWorkspaceBuild`/`_z42cMode`（A，行为保持）+ B 步翻默认 interp→jit：jit-fixpoint-check 4 平台确认 z42c workspace interp==jit 逐字节、jit 快 1.67-3.6×、信任基线经 User 拍板移到 cranelift/JIT、逃生舱 `Z42C_BUILD_MODE=interp` 保留、bootstrap-check 恒 interp 例外。A/B 各 CI 全平台绿 + 本地不动点 7/7。机制落 build.md「z42c 编译执行模式」）。历史：**~~fix-bootstrap-format-bump-deadlock~~** ✅ 已归档 2026-07-12（两代自举根治格式-bump CI 死结：ci-bootstrap 版本差 gate（读种子 zpkg minor vs 源码 `ZpkgWriterZ.Minor`）→ 不等则两代自举：SDK 旧 VM(`bin/z42vm`) 跑 Gen1（旧种子 z42c 编当前源→旧壳/新逻辑）+ Gen2（旧 VM 跑 gen1 z42c→新格式产物）→ 新 VM 接管；runtime/compile stdlib 分离（entry-dir 旧 / Z42_LIBS 新）解死锁（design D7）。**stage 3 由现实达成**：合入(8318ad7e)后 zpkg 0.25→0.30 连续 5+ 次真格式 bump 的 build-and-test/toolchain-bootstrap/package job **全绿**（唯二红为正交 android-emu flake；design 定「下一次真 bump 才是终极验证」，人造 bump 冗余）。文档 self-hosting Deferred 关闭 + version-bumping + bootstrap-seed 补两代机制。收尾 by 后续会话（User 授权），解锁 `consolidate-z42c-invocations`（§2.1 + JIT 切换）。历史：**~~fix-stdlib-test-missing-artifact-flaky~~** ✅ 已归档 2026-07-09（编译退出0却无产物→串行重试兜底 + 清晰硬错误;负载 Heisenbug 鲁棒性兜底,本地不可复现故不可验证根治,严格改进）。历史：**~~add-file-level-incremental~~** ✅ 已归档 2026-07-08（toolchain 部分：`test incremental` 暴力对账器 + CLI 注册；详见 compiler 行）。历史：**~~finish-xtask-review-cleanup~~** ✅ 已归档 2026-07-08：xtask_review §2 剩余安全项——§2.7 两套字符串排序合一（`_sortedStrings`→copy+`_sortStrings` 薄包装）+ §2.9 剩余 12 处 `z42-test-runner` 注释→z42b。copy 变体合一 / §2.1 / §2.5-2.8 留后续（需 compiler 干净树或更重验证）。历史：**~~consolidate-xtask-helpers~~** ✅ 已归档 2026-07-07（xtask_review.md §2 代码收敛安全子集——`_driverZpkg` helper retrofit 5 处字面量 0 残留（§2.2）+ `_assembleDriverHome`→`_compilerMembers`（§2.3 drift 防护，核对 default-members 同 7 成员集）+ 注释腐烂清理（§2.9：stdlib 头部/body C# 种子流程重写 z42c-only、`_ensureDriverVm`/xtask.z42 dotnet primer + 幽灵 helper 列表、test-runner 幽灵注释清晰项）+ 小修（§2.10：`_stageToolchain` `_procEnd`/`_testAll(false)`/`_mapFile` `scripts/` 前缀/`test dist` help both）。xtask.zpkg 重编 41/41 + bench --quick exit 0 + 命令树 -h smoke；纯内部收敛零行为变更。§2.1 workspace-build helper + §2.5/2.6/2.8 结构性重构留独立 change（需 fixpoint/packaging 验证，当前树含 compiler WIP 会污染）。历史：**~~fix-xtask-review-bugs~~** ✅ 已归档 2026-07-07（修 `docs/xtask_review.md` 复核仍活的 3 bug——test changed 映射 `test lib`→`test stdlib`（已无路由，改 stdlib/toolchain 文件时 exit 2；实测旧路由 exit 2 / 新路由 exit 0）+ bench `Directory.Enumerate` 未排序（common-pitfalls §1）→ `_sortedStrings` + vm/stdlib/compiler 三段手写 build-if-missing 且 compiler 无条件重建 → 收敛 `_ensureToolchainDeps`（实测 --quick 有序选集 01/02）+ release.yml 三处已删命令按 RID 分支迁移到 `package sdk/runtime/workload/index`（对齐 ci.yml 已验证形态）。xtask.zpkg 重编 41/41 通过；review #3 复核已由 4e8ea7d9 修好未处理。纯 dev-CLI/CI/文档，不触语言行为，未跑全 gate 见 tasks 备注）。历史：**~~add-vscode-syntax-ext~~** ✅ 已归档 2026-07-07（IDE 语法支持 A 期：devtools/vscode 声明式扩展（package.json + language-configuration + TextMate grammar）；grammar 关键字生成器驱动——Lexer SoT → `z42c --dump-keywords`（87 词注册序）→ xtask 分类表（六组）+ 穷尽校验（漏/幽灵/重复报错指名）→ 模板渲染 → `z42.tmLanguage.json` 入库；`xtask deps install vscode`（生成 + symlink `~/.vscode/extensions/z42.z42-lang`；win 打印指引）；新 GREEN stage `test vscode-syntax`（内存重渲染 diff，挂 `_testAll` 链尾 + 独立命令）。B 期 LSP（`z42d lsp` + 扩展升级 client）独立变更。GREEN：全 stage 含新 stage + 负路径（缺文件/stale 字节）+ 安装实测。compiler 部分见 compiler 行）。**~~simplify-xtask-deps~~** ✅ 已归档 2026-07-07（deps 命令面收敛为三正交子命令：`check [--os]` 唯一只读校验（presence + drift 单实现，删 `_checkAndroidDrift`/`_checkIosDrift`/`_firstIntAfter` 重复实现 ~60 行；wasm `_check(wpm,wpm)` 恒真假检查改真校验）/ `install [--os] [--force]` 纯安装（删 --check/--drift/--print-env 三 mode flag + node/android-emulator step positional）/ `env` 导出 ANDROID_NDK_HOME（stdout 纯净可 eval）。依赖两层模型：平台必备直装（node 划入 wasm 必备）+ 用到才装零命令面（emulator tier ~4GB 由 `test platform android run` 自动装、node 兜底由 wasm 测试自动装，失败即测试失败）。实施期修正：CI build-and-test 裸跑 `deps check` 当 drift 门禁 → 退出码策略 = drift 恒致败 / presence 仅显式 --os 时致败，CI 零 workflow 改动。GREEN：全 stage + smoke 正负路径 + wasm 必备链端到端（node v22.11.0 hermetic））。**~~redesign-xtask-test~~** ✅ 已归档 2026-07-07（test 命令面重构：`test vm`+`test cross-zpkg`→`test e2e`（src/tests 统一 + `--dir`/`--file`/`--mode` 子选择，两 harness 内部保留）/ 新 `test runtime`（cargo test `--test-threads=1`）/ `regen`→并入 `build test`（`_regenCore`/`_regenGolden` 保留）/ 删 `audit`（224 行正则补 using 鸡肋）/ 文件归位（xtask_regen→build/xtask_test_assets，xtask_release→package/）。**关键修订**：`test runtime` 原打算进 `test all`，实测 signal_handler_e2e crash-helper 在信号受限沙箱挂死（1h51m）→ 会卡死 always-run gate → 移出 gate，改每条 CI test-host 腿单独一步 + standalone + test changed 映射；顺带发现并修 Windows test-host（`test all` 本 `if!=windows`，其测试全靠被删的 Windows step）→ 恢复为 build test+test runtime。CI 迁移：删冗余 Windows cargo/regen step、vm/cross→e2e、regen→build test。实施发现 z42c bug：overloaded **自由函数** crash TSIG 导出（workaround 用 distinct 名）。stage-toolchain 评估=仍在关键路径不可裸删，收敛记 Deferred CO-D1（roadmap:351）。本地 GREEN：`xtask test`（e2e+stdlib+compiler，不含 runtime）全绿 + 自举 7/7；Windows/全腿 runtime/signal 测试待 CI 验）。**~~simplify-xtask-verify~~** ✅ 已归档 2026-07-07（收敛验证命令面：删 `test compiler-stdlib`（replace-csharp S2.1 dogfood 遗物，含 CI `compiler-stdlib` job）/ 合 `packages-{config,staging,assemble}`→`test packages`（3 命令→1，三函数保留）/ `bootstrap-check`→`test bootstrap`（移入 test 组）。顺带修 pre-existing bug：SDK 布局把 z42vm 挪 `bin/z42vm`，extract 检查仍找根目录 → 命令在解包即死；零拷贝 `--workspace --output-dir` 双轨实测破坏编译器兄弟包类型解析（E0402）已回退保留 per-member+runlibs（教训写入 build.md）。GREEN：`xtask test` 全 stage 绿 + `test packages` PASS + `test bootstrap` 双轨全过）。**~~merge-package-release~~** ✅ 已归档 2026-07-06（`package` 收成 name 驱动 router（sdk/runtime/workload/index），删 `release`；对齐 build compiler\|stdlib。profile 降为 --profile；平台包 = `package runtime --rid`；`package workload` dual-mode（label 合并 / 无 label 建 per-RID）；`package index` = 旧 gen-release-index。`_buildPackageCore` 抽 `_pkgSetupDir`/`_pkgFinish`，保留供 test dist/all。CI 6 处调用点迁移。CI 786deefe：host-package + package-android/ios/wasm + test-host ×4 全绿（唯一红 test-android-emu = 模拟器 boot 基建 flake，正交）。顺带修 2 个并行 change 遗留 CI 红：build sdk --no-build apphost 守卫（768660f2）、增量 cache Windows 崩溃非致命 + bootstrap 容忍（c640a31d/83a555b9）、zpkg pinned test 断言 22→23（786deefe）。含 remote codesigner-ui→z42-lang URL 迁移（d990617e））。**~~add-build-toolchain~~** ✅ 已归档 2026-07-05（新 `build {workload,toolchain,test}` + build sdk 补 apphost 完整性；对称 build compiler\|stdlib。SoT：toolchain 输出/publish 路径一律从组件 z42.toml 读（`_desktopPublishDir`/`_toolchainZpkg`），删 3 处 z42b 硬编码；删 build launcher（并入 toolchain）；publish_dir 一致性 + 泄漏产物 + gitignore；scratch 目录（stdlib-run/alllibs/e2e/selfhost-gen1/dogfood）移出 build/ → artifacts/.scratch/。本地 GREEN + build workload/toolchain/test/sdk 各跑通）。**~~simplify-compiler-build~~** ✅ 已归档 2026-07-05（xtask 自建/编 stdlib 改跑自包含 driver + `.stdlib-run` 快照，删 selfbuild-runlibs/dogfood；env 折叠 Z42_HOME + CI ci-bootstrap 同步；见 compiler 行）。**~~add-package-layout-config~~** ✅ 已归档 2026-07-01（配置驱动发行包布局：`packages.toml`（包=include 组件清单）+ bin/payload 部署布局 + xtask 从暂存根 `_pkgAssembleFromStaging` 组装 SDK/runtime 包（两包同一组装路径，无特例）；runtime 包裁定为纯嵌入式（native+stdlib，不含 z42c/z42vm host 工具）；devtools/interactive 注册进 packages.toml（占位实现）。端到端字节一致验证（5.1-5.4）受并行 add-params-varargs 会话 zpkg minor 22↔0.23 skew 阻断 → **Deferred** `packaging-future-byte-identical-verification`（roadmap:442 已索引）；重构逻辑经 `test packages-config`/`-staging`/`-assemble` 三自检 + 部分 e2e 独立验证）。**~~move-publish-to-z42b~~** ✅ 已归档 2026-07-01（build→z42c 转发 + publish 实现迁 z42b（launcher 转发，`Z42_APPHOST_TEMPLATE` 传 stub）+ publish build-if-needed（缺 zpkg spawn z42c）；z42b apphost patcher **内联**（builder_apphost.z42）保纯 stdlib 依赖——修实施中暴露的 z42b 测试运行器上下文 E0401。GREEN：stdlib [Test] 272/272 + 自举 7/7 + vm 192 + e2e 四路径全过 + SDK 零回归。含 add-package-layout-config 第①步的 bin/payload 模型+project.md）。**~~gate-apphost-on-config~~** ✅ 已归档 2026-06-30（apphost 改为 `[platform.desktop] apphost = true` 显式 gate——publish 唯有 apphost=true 才产 apphost，`publish_dir` 降为纯输出位置；touches stdlib `z42.project`(DesktopConfig.Apphost + ManifestLoader) + toolchain `launcher_export`(_platformBool + _cmdPublishDesktop gate) + 4 toml(xtask/launcher/z42b/z42c.driver) + docs；compiler 锁下仅 z42c.driver.toml 3 行 additive config，正交。GREEN：build stdlib + launcher 编译 + gate 行为 smoke 双向）。**~~organize-xtask-scripts~~** ✅ 已归档 2026-06-30（scripts/ 32 个 xtask_*.z42 按命令分 common/build/test/package/install 子目录 + 单文件命令留根 + toml 改 glob `**/*.z42` + README 刷新去 dotnet 残留补命令流程图；保留前缀仅移目录、纯搬移；GREEN：test compiler 7/7 + 不动点 7/7 + z42.math）。**~~compile-once-toolchain~~** ✅ 已归档 2026-06-30（再scope：高价值 CI 去冗余已落地——缓存根因修复 / current-sdk 拆出关键路径 / 并行 regen / package z42c 去重+`--no-build` / script-zero / 并发组+publish-nightly 串行化，CI ~47→~36min；见 `archive/2026-06-29-ci-speedup-and-script-zero` + `archive/2026-06-30-compile-once-toolchain`。正式分层模型——成对分代 gen1/2/3 / test 腿全消费 / 三发布门 / cross-bootstrap / 本地分阶段命令——经实测低性价比，移 Deferred，roadmap 已索引）。 **~~remove-dotnet-from-builds~~** ✅ 已归档 2026-06-26（replace-csharp S5 Phase A→C 全完成：BUILD 站点切 z42c → gate 切 z42c 不动点 → **删 C#**（src/compiler 旧 280 .cs + z42.Tests + z42.slnx；src/z42c 重命名为 src/compiler）+ 清全 5 workflow setup-dotnet/dotnet。硬前置 z42c golden parity 编通全 ~333 golden（da0d547b）；stdlib/bench/build-and-test 全 4 OS C#-free + bootstrap-no-csharp 闭环绿。**dotnet 已实质完全移除**。后续 CI 去冗余规划见 `docs/spec/changes/compile-once-toolchain/`（spec 定稿，暂不开工））。 **~~bootstrap-z42c-from-nightly~~** ✅ 已归档 2026-06-22（replace-csharp S4：脱 C# 重建 z42c 闭环。`scripts/selfhost-bootstrap.sh`（z42vm only，无 dotnet：种子 z42c 编 stdlib+z42c+xtask，不动点 7/7 逐字节一致）+ runtime package 携 z42c/ 种子 + CI job `bootstrap-no-csharp (linux-x64)` **端到端绿**（下载携种子 nightly→dotnet PATH-stub 屏蔽→C#-free 重建+不动点；滚动自愈走通）。flip `_buildStdlibCore` 脱 C# 移交 S5）。 **~~dogfood-z42c-stdlib-build~~** ✅ 已归档 2026-06-22（replace-csharp S3 生产接管：`_buildStdlibCore` 翻转 z42c——C# 种子 → build z42c → run-libs → z42c interp 重编 stdlib per-member → verify + flat view；`_testCompilerZ42Stdlib` 改调 `_csharpBuildStdlibSeed` 避免递归。full GREEN gate 全绿：C# 1571 / vm interp 169+jit 165 / cross-zpkg 2 / **stdlib 272/272 z42c-built** / compiler-z42 7/7 byte-identical+17 units+e2e。dogfood 共暴露并修复 8 个 z42c/stdlib bug，均独立归档：TSIG 可选参数 / generic-ctor multicast / strip-symbols BLID / blake3 多块 / native named-entry / cross-ns 静态调用 / 静态字段赋值 / ctor this-委托。铁律 C# 仍为种子未删）。**~~parallelize-and-jit-stdlib-tests~~** ✅ 已归档 2026-06-21（direct-DLL 编译 3× + stdlib unit 级并行 2×（193s→96s 本地）+ test-runner `--mode interp\|jit`（jit 强制 subprocess fork z42vm，in-process 无法驱动 JIT）+ 独立 CI job `stdlib-jit-consistency`；全量 stdlib 272/272 interp & jit）。**~~rework-host-runtime-pack~~** ✅ 已归档 2026-06-21（host runtime pack 从 SDK 复制 libs+native = 完整可嵌入运行时；修桌面 SDK 误装 42M ios .a；package+test dist 377/0）。**~~remove-examples-from-packaging~~** ✅ 已归档 2026-06-21（各发行包不再含 examples/；examples/ 源目录保留为夹具；6094b38f）。前序：**~~clarify-launcher-command-face~~ ✅ 已归档 2026-06-17**（实装 `z42 list --workloads`；命令面 `update`/`use`/`workload update` 标 planned——真特性需 latest/pin resolution 未建）。**~~dry-ci-release-index~~ ✅ 已归档 2026-06-17** |

## 全部 in-flight change（参考，子系统占用以上表为准）

| change | 子系统（待逐个确认） |
|--------|---------------------|
| `fix-bootstrap-format-bump-deadlock` | toolchain —— 🔴 全量 SPEC 已起草 2026-07-09（**不持锁,待 User 6.5**）:两代自举根治格式-bump 的 CI 引导死结(用 SDK 自带旧 VM 跑 Gen1/Gen2→新 VM 接管;版本差 gate 使日常零成本);落地 Deferred `single-vm-bootstrap-gap`。本地不可完整验证,靠人造-bump-分支验闭环 |
| ~~add-file-level-incremental~~ | compiler + toolchain —— ✅ 已归档 2026-07-08（文件级增量编译落地：对账器 47/47 byte-identical + D8 性能三轮修正持平；见子系统持有表 compiler 行详述）|
| ~~add-indexed-zpkg-min-patch~~ | compiler + runtime —— ✅ 已归档 2026-07-09（indexed 最小 patch 分发落地：主文件 FILE 段 + 自包含散装 zbc + VM 装载 + zpkg 0.24；见子系统持有表 compiler 行详述）|
| `add-partial-types` | compiler —— 🔴 DRAFT 已起草 2026-07-08（**排队中，未持锁**）：C# 风格 partial 类型（class/struct/record/interface + partial method C#9 形态）；方案 A 编译期合并单条 TYPE record → 零 zbc/zpkg 格式 bump、零 Rust loader 改动；碎片组增量联动失效、非 partial 文件不受影响。**前置**：`add-indexed-zpkg-min-patch` 释放 compiler 锁 ✅（2026-07-09 已释放，可进阶段 6.5）|
| ~~simplify-xtask-deps~~ | toolchain —— ✅ 已归档 2026-07-07（deps 三正交子命令 + 依赖两层模型 + emulator 零命令面自动装；详见子系统持有表 toolchain 行）|
| ~~add-vscode-syntax-ext~~ | compiler + toolchain —— ✅ 已归档 2026-07-07（VSCode 语法高亮扩展 A 期；详见两子系统持有表行；B 期 LSP 独立变更）|
| ~~retire-test-runner~~ | stdlib + toolchain + runtime —— ✅ 已归档 2026-06-30（删 Rust z42-test-runner，z42b 反射 runner 接管：`__invoke_static` 按 FQN 跑 free-function 测试 + `__load_module` 注册 import_namespaces 候选/force-load 依赖闭包/重跑 init_static_fields；z42b=test/bench host（builder_cli+builder_test）；xtask test stdlib/compiler 改 spawn z42b。CI 全平台绿含 Windows+JIT，272/272 + 自举 7/7。build/publish/export 编排留 wire-z42b-host-build。附带命名空间 z42.project→`Z42.Build.Project`：解 z42c.project 冲突的方向，但同名类跨 zpkg first-wins 串味，故暂不入 build，待 wire-z42b-host-build 让 z42c 改引用本库、删 z42c.project） |
| `add-workload-command-dispatch` | toolchain + stdlib（z42.cli）—— 🟡 SPEC READY，可立即实施（无前置）；**不持锁**，开工前重新登记 |
| `converge-z42c-onto-z42-project` | compiler（z42c.project 拆分 z42c.zpkg + 调用点迁移）+ stdlib（z42.project 登记 member + GREEN）—— 🔴 SPEC DRAFT 已起草 2026-07-10（wire-z42b 链的**地基**：删 z42c.project 的 manifest-model 重复、z42c 改引用 z42.project 组合式模型，根除 flat-libs 同名串味）；**关键未知=自举死结**：present-unconsumed 的 z42.project 是否炸自举，**阶段 0 spike 先实测**再定分阶段（两 nightly / 原子 / rename-前置）。**6.5 已批准（2026-07-10）但排队中**：compiler+stdlib 现被 in-flight 的 `add-enum-type-metadata` 占用（本地树有其未提交 WIP）→ 按子系统互斥锁排队，待其归档释放 compiler+stdlib 后再登记双锁 + 跑阶段 0 spike。**不持锁**|
| `wire-z42b-host-build` | stdlib（z42.build）+ compiler（Z42cCompiler）+ toolchain（z42b 登记 + launcher）—— 🟡 SPEC DRAFT（**前置刷新 2026-07-10**：toolchain 锁已释放、retire-test-runner 已归档；**新前置 `converge-z42c-onto-z42-project`**——z42.project member 登记移交它）；接入 PARKED 的 z42b → `z42 new`/`z42 build` 端到端；前置 extract-compile-pipeline-api（磁盘无容器）；**不持锁**，开工前重新登记。PARKED 代码已就位（08e840d5/5020b0d5/c1d068d2/98cebedd）|
| ~~add-boxing-conversions~~ | runtime —— ✅ 已归档 2026-06-29（装箱 prim↔object 方案 A：实证编译器侧已支持（GS6），整改动 = 运行期 Bool 拆箱恒等 + golden + 文档；z42c.semantics 零改动；vm interp 189/0 含 box_unbox。Method.Invoke 0.3.12 前置达成）|
| ~~add-method-invoke-non-generic~~ | runtime + stdlib + toolchain(harness) —— ✅ 已归档 2026-06-30（非泛型 MethodInfo.Invoke + Type.GetType：__method_invoke/__type_get_type builtin 复用 exec_function/make_type_from_name；异常原类型经 pending_thrown 透出 interp+jit；Activator 延后。root-cause 修 golden runner 继承陈旧 Z42_LIBS → xtask_test_vm 钉 fresh stdlib。GREEN：vm interp 190/0 含 method_invoke + jit 直验。解锁 retire-test-runner）|
| ~~add-type-based-overloads~~ | compiler（src/compiler/）—— ✅ 已归档 2026-07-01（type-based 方法重载决议，静态+实例，见 ACTIVE.md 子系统持有表 compiler 行详述；解锁 add-params-varargs 阶段 4）|
| ~~add-params-varargs~~ | compiler（src/compiler/）—— ✅ 已归档 2026-07-01（`params` 变长参数：typed `T[]`/`object[]` + normal/expanded 重载决议 + 跨包 TSIG，见 ACTIVE.md 子系统持有表 compiler 行详述；stdlib API 迁移归独立 follow-up）|
| ~~`extract-compile-pipeline-api`~~ | compiler —— ✅ 已归档 2026-07-26（核心 `PackageCompile` 库级 API 实际 2026-07-17 commit `1f3f9229` 落地、proposal/design 头标「已实施」，但当时漏补 tasks/漏 mv/漏更新本表 → 归档扫描补齐。DepScan scan-provider（CachedScan 复用）转 follow-up `perf-optimize-repl-eval` ②）|
| ~~restructure-publish-output-dirs~~ | compiler —— ✅ 已归档 2026-06-19 |
| ~~scaffold-z42c-selfhost~~ | z42c + compiler —— ✅ 已归档 2026-06-19（骨架完成；实现通过 port-z42c-* 系列）|
| ~~port-z42c-core~~ | z42c + toolchain —— ✅ 已归档 2026-06-07（见 spec/archive/2026-06-07-port-z42c-core）|
| ~~port-z42c-codegen~~ | z42c —— ✅ 已归档 2026-06-09（CG-1A–2，210 cases）|
| ~~port-z42c-zbc-writer~~ | z42c —— ✅ 已归档 2026-06-10（功能完整 .zbc writer + empty 逐字节 + e2e 四向；DBUG/span 移交 add-z42c-source-spans）|
| ~~add-z42c-source-spans~~ | z42c —— ✅ 已归档 2026-06-10（span→DBUG + byte-compare 3/3；7942ab7d）|
| ~~port-z42c-zpkg-build~~ | z42c —— ✅ 已归档 2026-06-10（z42c build 端到端+直跑；e1ff3503）|
| ~~port-z42c-tsig~~ | z42c —— ✅ 已归档 2026-06-10（zpkg 全文件 byte-identical 2/2；05e615cf）|
| ~~port-z42c-import~~ | z42c —— ✅ 已归档 2026-06-10（hello-stdlib byte-identical + import e2e；a1fa39d8）|
| ~~port-z42c-instance-import~~ | z42c —— ✅ 已归档 2026-06-11（textapp byte-identical；gate 4/4）|
| ~~port-z42c-char~~ | z42c —— ✅ 已归档 2026-06-11（charcheck byte-identical；zbc 4/4）|
| ~~port-z42c-try~~ | z42c —— ✅ 已归档 2026-06-11（trycheck byte-identical；zbc 5/5）|
| ~~port-z42c-interface~~ | z42c —— ✅ 已归档 2026-06-11（ifacecheck byte-identical；zbc 6/6）|
| ~~port-z42c-closures~~ | z42c —— ✅ 已归档 2026-06-11（closcheck byte-identical；zpkg 5/5）|
| ~~port-z42c-package-symbols~~ | z42c —— ✅ 已归档 2026-06-11（multifile byte-identical；zpkg 6/6）|
| ~~port-z42c-statics-arrays~~ | z42c —— ✅ 已归档 2026-06-13（sacheck byte-identical；zbc 7/7；commit 3774faa4）|
| inline-jit-safepoint-check | runtime（暂停，不占锁） |
| investigate-concurrent-gc-stale-mark-race | runtime（暂停，不占锁） |
| ~~add-export-command~~ | toolchain —— ✅ 已归档 2026-06-14（`z42 export ios/android/wasm`；`[platform.*]` toml；`runtimes/<rid>/<ver>/` 平台 SDK；launcher_export*.z42 4 文件；0292c3a3）|
| ~~split-release-runtime-package~~ | toolchain —— ✅ 已归档 2026-06-14（`z42-runtime-*` 独立包；SDK 当 launcher；release-index.json runtime/launcher 子键；9 RID；4bbbd01b）|
| ~~migrate-scripts-to-z42~~ | toolchain —— ✅ 已归档 2026-06-19（主体完成；release.yml 2 处 .sh 延后 — 见 docs/workflow/release.md Deferred migrate-future-release-scripts）|
| add-z42-wasm-playground | runtime? / toolchain?（待回填） |
| ~~add-reflection-mvp~~ | runtime + stdlib —— ✅ 已归档 2026-06-09（feat 30776fae）|
| ~~make-typeof-return-type~~ | compiler + runtime —— ✅ 已归档 2026-06-09（C2，option A）|
| ~~add-attribute-reflection~~ | compiler + runtime + stdlib —— ✅ C3a（class-level）已归档 2026-06-09（feat 56d9cefb + 1377bfdb）|
| ~~add-attribute-reflection-methods~~ | compiler + runtime + stdlib —— ✅ C3b（method-level）已归档 2026-06-09（SIGS attr refs，zbc 1.11/zpkg 0.13）|
| ~~add-apphost~~ | toolchain —— ✅ 已归档 2026-06-09（feat a3720a16；per-app 原生 apphost，framework-dependent + 本地优先 + macOS 重签名）|
| ~~add-launcher-install~~ | toolchain —— ✅ 已归档 2026-06-13（`z42 install/self-update`；manifest-first + SHA256 + tgz/zip 流式解压；install-z42.sh --system/--dest/--dry-run/--verbose；删 install.sh；017c8116）|
| ~~fix-dist-runner-test-dirs~~ | toolchain —— ✅ 已归档 2026-06-09（dist runner 跳过 [Test]-based lib 测试 dir，修 secp256k1 误失败；镜像 GoldenTests.ContainsTestRunnerAttribute）|
| ~~fix-fqn-class-resolution~~ | compiler + stdlib —— ✅ 已归档 2026-06-09（ResolveMemberType namespace-aware FQN→类；移除 C3b FindByType workaround；GoldenTests 1545/1545）|
| ~~add-reflection-properties~~ | runtime + stdlib —— ✅ 已归档 2026-06-09（`Type.GetProperties()` + `PropertyInfo`，纯 runtime 派生自 get_/set_，无 zbc 格式 bump；GoldenTests 1549/1549）|
| ~~add-reflection-type-flags~~ | compiler + runtime + stdlib —— ✅ 已归档 2026-06-10（`Type.IsAbstract`/`IsSealed`；zbc 1.12 / zpkg 0.14，TYPE section flags 字节；cargo 795/0 + GoldenTests 1552/1552）|
| ~~add-cli-nested-subcommands~~ | stdlib —— ✅ 已归档 2026-06-10（`Std.Cli` 嵌套 `AddRouter`/`Resolve`/`CommandResolution`；14 新 [Test]；GREEN 269 文件/22 lib）。② xtask/launcher 迁移（合流 migrate-scripts-to-z42）解锁可开 |
| ~~add-reflection-static-fields~~ | compiler + runtime + stdlib —— ✅ 已归档 2026-06-10（`GetFields()` 含静态 + `FieldInfo.IsStatic`；zbc 1.13 / zpkg 0.15，TYPE section 静态字段块；cargo 797/0 + GoldenTests 1553/1553）|
| ~~add-cli-optional-positional~~ | stdlib —— ✅ 已归档 2026-06-10（`ArgParser.AddOptionalPositional`，z42.cli；11 新 [Test]）。migrate-xtask 前置 |
| ~~fix-reflection-test-compile~~ | stdlib + docs —— ✅ 已归档 2026-06-10（add-field 遗留 reflection.z42 编译失败：`none` 关键字 + 限定基名 `: Std.Attribute` upcast；改 `noneAttrs` + unqualified `: Attribute`；dotnet test 1554/1554。根因 compiler FQN-upcast bug 入 reflection.md Deferred `attr-factory-qualified-base-upcast`）|
| ~~migrate-xtask-launcher-to-std-cli~~ | toolchain —— ✅ 已归档 2026-06-10（xtask+launcher → Std.Cli 嵌套 router；`package`/`feature-matrix` 提顶层；删 `lib` 别名；每层 help；CI build package→package ×5。xtask GREEN 270/22 + test dist 347/0）|
| ~~add-field-attribute-reflection~~ | compiler + runtime + stdlib —— ✅ 已归档 2026-06-10（字段级用户 attribute 反射 `FieldInfo.GetCustomAttributes()`；zbc 1.14 / zpkg 0.16；cargo 799/0 + GoldenTests 1554/1554；参数 attr = follow-up）|
| ~~add-reflection-value-record-flags~~ | runtime + stdlib —— ✅ 已归档 2026-06-10（`Type.IsValueType` / `Type.IsRecord`，读 type-flags 已捕获的 struct/record 位；**无格式 bump**；cargo 800/0 + GoldenTests 1554/1554）|
| ~~fix-chained-property-dispatch~~ | compiler —— ✅ 已归档 2026-06-10（链式 getter 派发 `obj.GetType().BaseType.Name`；唯一根因 P1=Object stub GetType 返 Unknown→改取真实 Std.Type；纯 typecheck，无 zbc 格式/字节漂移；dotnet GoldenTests 1555/1555 + 链式 e2e 全过）|
| ~~add-parameter-attribute-reflection~~ | compiler + runtime + stdlib + z42c —— ✅ 已归档 2026-06-10（参数级用户 attribute 反射 `ParameterInfo.GetCustomAttributes()`；zbc 1.15 / zpkg 0.17，SIGS 每参数 attr-ref 块；z42c writer 同步；dotnet 1556/1556 + param_attributes.z42 e2e + cargo 757+21 + format 78。xtask gate 阻塞于 pre-existing 多文件 project-build 命名空间双重限定 bug → User 裁决单独 fix change 跟踪，本 change 走 dotnet 权威门）|
| ~~fix-multifile-project-namespace-qualify~~ | compiler —— ⚠️ **误判更正 2026-06-11**：xtask `undefined fn Z42Xtask.Std.Int32.Parse` 是**陈旧/混版产物幻影**，非真 bug。干净 worktree（origin/main）全量重建后 `xtask test vm`=346/0、零 undefined-function。gate 在干净树正常。仅存真 bug=niche 命名空间限定自由调用 `ns.func()`→`ns.ns.func`（xtask/常规不触发，低优先，待独立评估）。详见 memory `reference_multifile_project_namespace_double_qualify_bug`|
| ~~align-type-memberinfo-hierarchy~~ | stdlib + runtime —— ✅ 已归档 2026-06-11（`Std.Type : MemberInfo`；`typeof(C) is MemberInfo` 真，`Name` 由基类统一提供；短名基类无需编译器改动、无格式 bump；dotnet 1557/1557 + type_is_memberinfo.z42 e2e + cargo 759+21）|
| ~~fix-stale-build-stdlib-sh-refs~~ | runtime + compiler + toolchain —— ✅ 已归档 2026-06-11（报错/注释里 `./scripts/build-stdlib.sh` → `z42 xtask.zpkg build stdlib`；该脚本已折进 xtask；cargo+dotnet build 绿）|
| ~~fix-namespace-qualified-free-call~~ | compiler —— ✅ 已归档 2026-06-11（`ns.func()` 命名空间限定自由调用此前误绑 static-call-on-class `ns` → 双重限定 `ns.ns.func` 运行期 undefined；修 BindMemberCallOnUnknownTarget 识别同 ns 自由调用 → Free call；dotnet 1558/1558 + packed repro exit 0。即 fix-multifile-project-namespace-qualify 幻影下暴露的唯一真 bug）|
| ~~slim-instruction-enum~~ | runtime —— ✅ 已归档 2026-06-11（`Instruction` enum 96B→32B：装箱 15 个带 String 冷变体 `Variant(Box<XxxInsn>)`，热算术变体 inline；internally-tagged newtype 自动摊平保 JSON wire format；**无 zbc bump**，fixture 6/6 无字节 delta；cargo 806/0 + vm goldens 346/0）|
| ~~cache-cross-zpkg-call-target~~ | runtime —— ✅ 已归档 2026-06-11（review.md C7：per-site `OnceLock<Arc<Function>>` cross-zpkg Call 目标缓存，平行 method_tokens；首次解析后借用，消除每调用 `try_lookup_function` String hash；本模块快路径零额外开销；OnceLock write-once、per-site 无全局并发表；**无格式 bump**；cargo lib 764/0 + vm goldens 346/0 + cross-zpkg 2/0）|
| ~~add-reflection-inherited-static-fields~~ | runtime —— ✅ 已归档 2026-06-11（`Type.GetFields()` 含继承静态字段：`builtin_type_fields` 沿 base 链聚合祖先类 static_fields；对齐 C# 默认含继承公共静态；无格式 bump；dotnet 1559/1559 + inherited_static_fields.z42 e2e + cargo 764+21）|
| ~~add-reflection-parameter-names~~ | runtime —— ✅ 已归档 2026-06-11（`ParameterInfo.Name` 返真实源参数名：`resolve_func_sig` 读 Function DBUG local-vars（reg==参数索引），无符号回落 `arg{n}`；无格式 bump；dotnet 1560/1560 + parameter_names.z42 e2e + cargo 764+21）|
| ~~add-collection-contract-phase2~~ | stdlib —— ✅ 已归档 2026-06-11（review.md S2.4 Phase 2：`IBasicCollection<T>.AddOne(T)` + 5 集合委托自然 add；`BasicCollectionContract` add→count→clear 生命周期（distinct 元素普适 SortedSet 去重）；新增 Queue/Stack contract [Test]；无格式 bump；test stdlib z42.collections 5/5 + z42.test 5/5）|
| ~~add-reflection-array-element-type~~ | compiler + runtime + stdlib —— ✅ 已归档 2026-06-14（数组运行期不擦除 `GcRef<ArrayObj{element_type,elems}>`；`Type.IsArray`/`GetElementType()` + 非擦除 `arr.GetType()`；zbc 1.16/zpkg 0.18 ArrayNew/Lit element_type FQ 名；dotnet 1561/1561 + array_element_type.z42 e2e interp+jit + cargo lib 807 + 集成 native/zbc_compat 全绿 + xtask vm 354/cross-zpkg 2/stdlib 272）。**z42c writer 同步延后**（User 决策"先实现，延后 z42c"；当时 z42c 锁被占）→ `xtask test compiler-z42` byte-identical gate 暂红，follow-up 跟踪（见 memory `project_z42c_selfhosting`）|
| ~~add-reflection-get-interfaces~~ | compiler + runtime + stdlib —— ✅ 已归档 2026-06-14（`Type.GetInterfaces()` 含继承接口；zbc 1.17/zpkg 0.19 TYPE section 类接口块；base-walk 聚合 + 按名 dedup；dotnet 1562/1562 + get_interfaces.z42 e2e interp+jit + cargo lib 807 + 集成 zbc_compat/native 全绿 + xtask vm 356/cross-zpkg 2/stdlib 272）。z42c writer 接口块镜像延后（follow-up，同 array-element-type 处理）|
| ~~plan-0.3.x-three-streams~~ | docs —— ✅ 已归档 2026-06-19（五项扩展重排：boxing 0.3.11 + Method.Invoke 非泛型 0.3.12 + test-runner 删除 0.3.13 + CI三平台 0.3.13 + workload B1/B2 0.3.14；REPL 后移 0.3.15；soak 0.3.16）|
| ~~consolidate-platform-into-workload~~ | docs —— ✅ 已归档 2026-06-19（S0–S5 机械整合弧全完成；B 子系统设计转 workload-subsystem-future 归档）|
| ~~build-workload-subsystem~~ | docs —— ✅ 已归档 2026-06-19（B1–B5 全延后；延后条目入 launcher-command-dispatch.md Deferred；与 impl-command-discovery 合并为 workload-subsystem-future）|
| ~~impl-workload-install~~ | toolchain —— ✅ 已归档 2026-06-17（三平台 runtime/workload 分包 + `z42 workload install/list/remove`；本地 produce+install 端到端 iOS/wasm/android；多 RID 增量装；后续 B2-4 CI/B1/B4/B5 入 roadmap Deferred）|
| ~~impl-command-discovery~~ | docs —— ✅ 已归档 2026-06-19（B1 DRAFT；与 build-workload-subsystem 合并为 workload-subsystem-future；延后至 B2 完成后）|
| ~~runtime-dynamic-load-call~~ | runtime + stdlib —— ✅ 已归档 2026-06-19（接口落地；VM builtin 延后——前置：反射 MVP + 自举）|
