# 字面量发码站点快照（enforce-diagnostic-code-uniqueness 规则 ④，双向棘轮：多一条/少一条都红）。
# 发射点用字面量而非 DiagnosticCodes 常量，是 bootstrap-seed.md 分阶段引入纪律的**过渡期形态**：
# 新常量与引用它的发射点不能同 PR（上一版 z42c 的 z42c.core 里还没有这个常量）⇒ 中间必有一轮字面量。
# 所以这份清单的稳态是「上一个 nightly 以来新加的码」那么几条，**不是空**——④ 是常设配套，不会退役。
# 每条都是**欠账**：等它的常量随 nightly 进了种子，就该把发射点切回 DiagnosticCodes.<Name> 并重跑 --update。
# 重生成：`xtask test diagcodes --update`（改之前先想清楚你是不是在给一个已有码挂第二个含义）。
E0485 src/compiler/z42c.semantics/src/ModuleInitScan.z42 2026-09-23
E0486 src/compiler/z42c.semantics/src/ModuleInitScan.z42 2026-09-23
E0487 src/compiler/z42c.semantics/src/ModuleInitScan.z42 2026-09-23
