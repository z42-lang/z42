// apps/hello/src/Main.z42 — 调用 greeter 演示跨 member 依赖
namespace Hello;
using Std.IO;

using Greeter;

public static class Hello {
    public static int main() {
        var msg = Greeter.greet("z42 workspace");
        Console.println(msg);
        return 0;
    }
}
