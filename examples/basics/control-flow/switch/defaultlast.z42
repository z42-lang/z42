using Std.IO;

void Main() {
    // ANCHOR: code
    int n = 2;

    switch (n) {
        default: Console.WriteLine("其它");   // 只在两个 case 都不匹配时才走
        case 1:  Console.WriteLine("一");
        case 2:  Console.WriteLine("二");     // 命中这条
    }
    // ANCHOR_END: code
}
