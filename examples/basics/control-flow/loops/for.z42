using Std.IO;

void Main() {
    // ANCHOR: code
    // 三段：初始化 ; 继续条件 ; 每轮结束后做什么
    for (int i = 0; i < 3; i = i + 1) {
        Console.WriteLine($"i={i}");
    }

    // 第三段写 i++ 也一样
    for (int j = 0; j < 2; j++) {
        Console.WriteLine($"j={j}");
    }
    // ANCHOR_END: code
}
