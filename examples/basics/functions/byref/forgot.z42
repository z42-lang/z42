using Std.IO;

void Increment(ref int x) {
    x = x + 1;
}

void ByValue(int x) {
    x = 999;
}

void Main() {
    int a = 0;
    Increment(a);        // ✗ 漏写 ref
    Console.WriteLine(a);

    int b = 0;
    ByValue(ref b);      // ✗ 形参是按值传的，不该写 ref
    Console.WriteLine(b);
}
