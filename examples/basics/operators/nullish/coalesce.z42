using Std.IO;

void Main() {
    // ANCHOR: code
    string? name = null;

    string shown = name;
    if (shown == null) { shown = "(匿名)"; }   // 取不到就用备用值

    Console.WriteLine(shown);
    // ANCHOR_END: code
}
