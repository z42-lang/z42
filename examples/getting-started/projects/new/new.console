# ANCHOR: new
$ z42 new greeter
Created executable project `greeter` in greeter/

  cd greeter
  z42 run
# ANCHOR_END: new
# ANCHOR: files
$ cd greeter
$ ls
README.md
src/
z42.toml
$ cat z42.toml
[project]
name = "greeter"
version = "0.1.0"
kind = "exe"

[sources]
include = ["src/**/*.z42"]
$ cat src/Main.z42
namespace Greeter;

using Std.IO;

void Main() {
    Console.WriteLine("Hello, World!");
}
# ANCHOR_END: files
