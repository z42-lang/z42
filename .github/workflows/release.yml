name: Release

# Tag-triggered cross-platform SDK package publish.
#
# Triggers on tag matching v[0-9]+.[0-9]+.[0-9]+* (e.g. v0.2.5, v0.2.5-rc1).
# Pre-1.0 versions and tags containing '-' are published as GitHub prereleases.
#
# Pipeline: verify (tag ↔ versions.toml) → package × 9 RIDs → publish (with SHA256SUMS).
# See docs/spec/changes/add-release-automation/ (until archived) or
# docs/spec/archive/<date>-add-release-automation/.

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (must match versions.toml [project].version, e.g. 0.1.0)'
        required: true
        type: string

permissions:
  contents: write   # gh release create

jobs:
  # ── 1. Verify tag matches versions.toml [project].version ──────────────
  verify:
    name: verify-version(linux-x64)
    timeout-minutes: 15
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.extract.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract + verify version
        id: extract
        shell: bash
        run: |
          set -euo pipefail
          if [ "$GITHUB_EVENT_NAME" = "push" ]; then
            tag="${GITHUB_REF#refs/tags/v}"
          else
            tag="${{ inputs.version }}"
          fi
          # read versions.toml [project].version (no versions.sh; this job has no
          # z42 toolchain — same awk pattern as scripts/install-z42.sh). Split on
          # the quote so a trailing comment (`version = "0.2.0"  # ...`) doesn't
          # leak into the value (the old gsub /.*"|".*/ kept the comment).
          repo_version=$(awk '
            /^\[project\]/ {inblock=1; next}
            /^\[/ {inblock=0}
            inblock && /^version/ {split($0, a, "\""); print a[2]; exit}
          ' versions.toml)
          if [ "$tag" != "$repo_version" ]; then
            echo "drift: tag=$tag versions.toml=$repo_version" >&2
            exit 1
          fi
          echo "version=$tag" >> "$GITHUB_OUTPUT"
          echo "✓ release version: $tag"

  # ── 2. Build per-RID SDK package + archive ─────────────────────────────
  package:
    name: package-${{ matrix.rid }}
    timeout-minutes: 60
    needs: verify
    strategy:
      fail-fast: false
      matrix:
        include:
          - { rid: linux-x64,      runs-on: ubuntu-latest,      rust-target: x86_64-unknown-linux-gnu }
          - { rid: linux-arm64,    runs-on: ubuntu-24.04-arm,   rust-target: aarch64-unknown-linux-gnu }
          - { rid: macos-arm64,    runs-on: macos-15,           rust-target: aarch64-apple-darwin }
          - { rid: windows-x64,    runs-on: windows-latest,     rust-target: x86_64-pc-windows-msvc }
          - { rid: ios-arm64,      runs-on: macos-15,           rust-target: aarch64-apple-ios }
          - { rid: iossim-arm64,  runs-on: macos-15,           rust-target: aarch64-apple-ios-sim }
          - { rid: android-arm64,  runs-on: ubuntu-latest,      rust-target: aarch64-linux-android }
          - { rid: android-x64,    runs-on: ubuntu-latest,      rust-target: x86_64-linux-android }
          - { rid: browser-wasm,   runs-on: ubuntu-latest,      rust-target: wasm32-unknown-unknown }
    runs-on: ${{ matrix.runs-on }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust + target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      - name: Cache Rust deps (Swatinem)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/runtime -> ../../artifacts/build/runtime
          shared-key: release-${{ matrix.rid }}

      # Android-only: cargo-ndk + NDK r26d
      - name: Install cargo-ndk
        if: startsWith(matrix.rid, 'android-')
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-ndk

      # local-cache: false — local-cache: true produced an incomplete
      # ANDROID_NDK_HOME directory (clang missing under toolchains/llvm/prebuilt/),
      # causing libffi-sys's configure to fail with "C compiler cannot create
      # executables". Fresh download every run is ~2 min slower but reliable.
      - name: Setup Android NDK r26d
        if: startsWith(matrix.rid, 'android-')
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r26d
          local-cache: false

      # wasm-only: wasm-pack + wasm-tools
      - name: Install wasm-pack + wasm-tools
        if: matrix.rid == 'browser-wasm'
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-pack,wasm-tools

      # build the host toolchain from the prev nightly's z42c
      # seed (same path as CI build-and-test), then the package steps below run via
      # `z42vm xtask.zpkg`. The host RID is derived from the runner; the cross target
      # (matrix.rust-target) is handled by `xtask package`.
      - name: Bootstrap toolchain (nightly z42c seed → current source)
        uses: ./.github/actions/ci-bootstrap
        with:
          gh-token: ${{ secrets.GITHUB_TOKEN }}

      # wasm-only: pre-build pkg-web/ + pkg-nodejs/ via wasm-pack (WasmBackend)
      - name: Build wasm platform
        if: matrix.rid == 'browser-wasm'
        shell: bash
        run: |
          set -euo pipefail
          vm="$PWD/artifacts/build/runtime/release/z42vm"
          Z42_PORTABLE_VM="$vm" Z42_LIBS="$PWD/artifacts/build/libraries/dist/release" \
            "$vm" artifacts/xtask/xtask.zpkg -- test platform wasm build

      - name: Package ${{ matrix.rid }} (z42 xtask.zpkg package)
        shell: bash
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          set -euo pipefail
          vm="$PWD/artifacts/build/runtime/release/z42vm"; [ -f "$vm.exe" ] && vm="$vm.exe"
          rid="${{ matrix.rid }}"
          libs="$PWD/artifacts/build/libraries/dist/release"
          # Windows: `package` cargo-relinks z42vm and can't overwrite the *running*
          # z42vm.exe → run xtask from a copy outside the cargo output dir (mirrors
          # ci.yml host-package). No-op on Linux/macOS.
          runner="$vm"
          if [ "${RUNNER_OS:-}" = "Windows" ]; then
            mkdir -p "$PWD/.ci-runner"; cp "$vm" "$PWD/.ci-runner/"
            cp "$PWD"/artifacts/build/runtime/release/*.dll "$PWD/.ci-runner/" 2>/dev/null || true
            runner="$PWD/.ci-runner/$(basename "$vm")"
          fi
          # merge-package-release: the former combined `package release --rid` split
          # into name-driven subcommands. Desktop host RID → SDK + runtime + desktop
          # workload (--no-build consumes the bootstrap-built z42c+stdlib); platform
          # RID (ios/android/wasm) → runtime pack + tooling facade.
          if [[ "$rid" == linux-* || "$rid" == macos-* || "$rid" == windows-* ]]; then
            Z42_PORTABLE_VM="$vm" Z42_LIBS="$libs" "$runner" artifacts/xtask/xtask.zpkg -- package sdk --no-build
            Z42_PORTABLE_VM="$vm" Z42_LIBS="$libs" "$runner" artifacts/xtask/xtask.zpkg -- package runtime --no-build
            Z42_PORTABLE_VM="$vm" Z42_LIBS="$libs" "$runner" artifacts/xtask/xtask.zpkg -- package workload
            # test workload = payload-only, platform-agnostic → build once on the
            # primary desktop host (package-test-workload). Version read from versions.toml.
            if [[ "$rid" == "macos-arm64" ]]; then
              Z42_PORTABLE_VM="$vm" Z42_LIBS="$libs" "$runner" artifacts/xtask/xtask.zpkg -- package workload test
            fi
          else
            Z42_PORTABLE_VM="$vm" Z42_LIBS="$libs" "$runner" artifacts/xtask/xtask.zpkg -- package runtime --rid "$rid"
          fi

      - name: Archive
        shell: bash
        run: |
          set -euo pipefail
          rid="${{ matrix.rid }}"; version="${{ needs.verify.outputs.version }}"
          mkdir -p artifacts/release

          _tar() {
            local src="$1" dst="$2"
            if [[ "$rid" == windows-* ]]; then
              # NOTE: `shell: bash` on windows-* is Git Bash → GNU tar, which CANNOT
              # write ZIP (`--auto-compress` only knows .gz/.xz/.zst/… so `tar -a -cf
              # x.zip` silently produces a *tar* named .zip → downstream `unzip` fails
              # with "End-of-central-directory signature not found", and users get a
              # broken SDK/runtime .zip). Use PowerShell Compress-Archive for a real ZIP.
              powershell -NoProfile -Command "Compress-Archive -Path 'artifacts/packages/$src/*' -DestinationPath 'artifacts/release/${dst}.zip' -Force"
            else
              tar -C "artifacts/packages/$src" -czf "artifacts/release/${dst}.tar.gz" .
            fi
          }
          # rid → workload name (ios/android/wasm); "" for desktop.
          _wl_of() {
            case "$1" in
              ios-*|iossim-*) echo ios ;;
              android-*)      echo android ;;
              browser-wasm)   echo wasm ;;
              *)              echo "" ;;
            esac
          }
          # The single RID per platform that ships the (identical across ABIs/slices)
          # workload tooling archive — avoids same-name artifact collisions at merge.
          _is_primary() { case "$1" in ios-arm64|android-arm64|browser-wasm) return 0 ;; *) return 1 ;; esac; }

          if [[ "$rid" == linux-* || "$rid" == macos-* || "$rid" == windows-* ]]; then
            # Desktop: SDK + runtime + desktop workload (apphost stub, per-host RID;
            # Decision 9 full gating — apphost no longer baked into the SDK).
            _tar "z42-${version}-${rid}-release" "z42-sdk-${version}-${rid}"
            echo "✓ sdk: z42-sdk-${version}-${rid}"
            _tar "z42-runtime-${version}-${rid}" "z42-runtime-${version}-${rid}"
            echo "✓ runtime: z42-runtime-${version}-${rid}"
            _tar "z42-workload-${version}-desktop-${rid}" "z42-workload-${version}-desktop-${rid}"
            echo "✓ desktop workload: z42-workload-${version}-desktop-${rid}"
            # test workload (payload-only) built only on macos-arm64 → archive once.
            if [[ "$rid" == "macos-arm64" ]]; then
              _tar "z42-workload-${version}-test" "z42-workload-${version}-test"
              echo "✓ test workload: z42-workload-${version}-test"
            fi
          else
            # Platform RID (ios/android/wasm) — runtime/workload split (B2-3/B2-4):
            #   runtime pack (per RID): z42-runtime-<v>-<rid>/  → z42-runtime-<v>-<rid>
            #   workload tooling (primary RID only, identical across ABIs/slices):
            #     z42-<v>-<rid>-release/ → z42-workload-<v>-<wl>
            _tar "z42-runtime-${version}-${rid}" "z42-runtime-${version}-${rid}"
            echo "✓ runtime: z42-runtime-${version}-${rid}"
            if _is_primary "$rid"; then
              wl="$(_wl_of "$rid")"
              _tar "z42-${version}-${rid}-release" "z42-workload-${version}-${wl}"
              echo "✓ workload: z42-workload-${version}-${wl}"
            fi
          fi

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.rid }}
          path: artifacts/release/z42-*
          retention-days: 7
          if-no-files-found: error

  # ── 3. Publish GitHub Release with all 9 archives + SHA256SUMS ─────────
  publish:
    name: publish-release(linux-x64)
    timeout-minutes: 45
    needs: [verify, package]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Provision z42vm + xtask.zpkg so the release-packaging steps run as
      # `xtask release …` (script-zero — no scripts/release/*.sh). Bootstrap
      # from the prev nightly seed, same as the package job. Host = linux-x64.
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust deps (Swatinem)
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src/runtime -> ../../artifacts/build/runtime
          shared-key: release-publish
      - name: Provision z42vm + xtask (bootstrap from prev nightly seed)
        uses: ./.github/actions/ci-bootstrap
        with:
          rid: linux-x64
          gh-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download all archives
        uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: 'release-*'
          merge-multiple: true

      - name: List archives
        run: ls -la dist/

      # Merge the 4 per-RID desktop-workload pieces (each carries its apphost-<rid>)
      # into ONE RID-agnostic z42-workload-<v>-desktop.tar.gz so any host can
      # `publish --rid <target>` (cross-platform apphost output). Removes the
      # per-RID intermediates so only the merged archive ships in the release.
      - name: Assemble single desktop workload
        shell: bash
        run: |
          Z42_PORTABLE_VM="$PWD/artifacts/build/runtime/release/z42vm" \
          Z42_LIBS="$PWD/artifacts/build/libraries/dist/release" \
            artifacts/build/runtime/release/z42vm artifacts/xtask/xtask.zpkg -- \
            package workload "${{ needs.verify.outputs.version }}" dist

      - name: Generate SHA256SUMS
        shell: bash
        run: |
          set -euo pipefail
          ( cd dist && shasum -a 256 z42-*.tar.gz z42-*.zip 2>/dev/null | sort -k2 ) > dist/SHA256SUMS
          echo "--- SHA256SUMS ---"
          cat dist/SHA256SUMS

      - name: Generate release-index.json
        shell: bash
        run: |
          v="${{ needs.verify.outputs.version }}"
          Z42_PORTABLE_VM="$PWD/artifacts/build/runtime/release/z42vm" \
          Z42_LIBS="$PWD/artifacts/build/libraries/dist/release" \
            artifacts/build/runtime/release/z42vm artifacts/xtask/xtask.zpkg -- \
            package index "$v" dist stable "v$v" "$v"

      - name: Create GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          v="${{ needs.verify.outputs.version }}"
          # Prerelease if version contains '-' (e.g. -rc1) OR is strictly < 1.0.0.
          prerelease_flag=""
          if [[ "$v" == *-* ]]; then
            prerelease_flag="--prerelease"
          elif [ "$v" != "1.0.0" ] \
            && [ "$(printf '%s\n1.0.0\n' "$v" | sort -V | head -1)" = "$v" ]; then
            prerelease_flag="--prerelease"
          fi
          gh release create "v$v" $prerelease_flag --generate-notes \
            --title "z42 v$v" \
            dist/SHA256SUMS dist/release-index.json dist/z42-*
