// [ModuleInit] 标在**实例**方法上 ⇒ E0486（包初始化器无 this）。
namespace Demo.ModBadApp;

using Std.IO;
using Demo.ModBad;

public class Boot {
    [ModuleInit]
    void Init() { Console.WriteLine("nope"); }
}

void Main() { Console.WriteLine(Answer().ToString()); }
