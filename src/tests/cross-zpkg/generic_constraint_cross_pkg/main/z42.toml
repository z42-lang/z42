[project]
name = "demo.capp"   # 命名为 *app 使其在 dist glob 中排在 demo.cext/demo.ctarget 之前
                     # ——cross-zpkg 驱动取字母序首个 zpkg 当 exe 运行（约定：exe 排首）
version = "0.1.0"
kind = "exe"
entry = "DemoCMain.Main"
pack = true
[dependencies]
"demo.ctarget" = "0.1.0"
"demo.cext" = "0.1.0"
