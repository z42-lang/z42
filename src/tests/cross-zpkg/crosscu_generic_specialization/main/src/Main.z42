namespace Demo.CrossCuApp;

using Std;
using Std.IO;
using Demo.CrossCuTarget;

void Main() {
    Console.WriteLine(UseSameFile());
    Console.WriteLine(UseOtherFile());
    Console.WriteLine(ConstructOtherFile());
    Console.WriteLine(UseVirtual());
}
