namespace Demo;

abstract class Shape {
    abstract string Describe();
}

class Circle : Shape {
    int Radius;

    public Circle(int r) {
        this.Radius = r;
    }

    override string Describe() {
        return $"circle(r={this.Radius})";
    }
}

class Rectangle : Shape {
    int W;
    int H;

    public Rectangle(int w, int h) {
        this.W = w;
        this.H = h;
    }

    override string Describe() {
        return $"rect({this.W}x{this.H})";
    }
}

void Main() {
    var c = new Circle(5);
    var r = new Rectangle(3, 4);

    // is operator
    Assert.True(c is Circle);
    Assert.True(c is Shape);
    Assert.False(c is Rectangle);

    // as operator
    var s = c as Shape;
    Assert.True(s is Shape);
}
