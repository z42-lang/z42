# z42c.semantics ParallelFor 工作池单测（parallelize-package-compile）。
[project]
name    = "z42c.semantics.test.parallel-for"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.semantics" = "0.1.0"
"z42c.syntax"    = "0.1.0"
"z42c.core"      = "0.1.0"

[build]
# Build output → artifacts mirror (mirror-build-output-per-component).
output_dir = "../../../../../artifacts/build/compiler/z42c.semantics/tests/parallel-for"
