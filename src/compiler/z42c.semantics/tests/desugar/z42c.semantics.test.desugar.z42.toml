# z42c.semantics BenchmarkDesugar 单测（fix-benchmark-bencher-arg-trampoline）。
[project]
name    = "z42c.semantics.test.desugar"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.semantics" = "0.1.0"
"z42c.syntax"    = "0.1.0"
"z42c.core"      = "0.1.0"

[build]
# Build output → artifacts mirror (mirror-build-output-per-component).
output_dir = "../../../../../artifacts/build/compiler/z42c.semantics/tests/desugar"
