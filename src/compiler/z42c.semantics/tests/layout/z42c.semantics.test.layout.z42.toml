# z42c.semantics StructLayout 单测（add-struct-value-semantics P1 阶段 1）。
[project]
name    = "z42c.semantics.test.layout"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.semantics" = "0.1.0"
"z42c.core"      = "0.1.0"

[build]
# Build output → artifacts mirror (mirror-build-output-per-component, 2026-06-16):
# src/compiler/z42c.semantics/tests/layout → artifacts/build/compiler/z42c.semantics/tests/layout (no src pollution).
output_dir = "../../../../../artifacts/build/compiler/z42c.semantics/tests/layout"
