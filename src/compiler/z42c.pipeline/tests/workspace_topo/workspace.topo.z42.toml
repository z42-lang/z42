# z42c.pipeline WorkspaceBuild 拓扑单测（z42c-build-workspace Milestone 2 task 1.5）。
[project]
name    = "z42c.pipeline.test.workspace_topo"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.pipeline" = "0.1.0"
"z42c.project"  = "0.1.0"
"z42c.ir"       = "0.1.0"
"z42c.core"     = "0.1.0"

[build]
# Build output → artifacts mirror (mirror-build-output-per-component, 2026-06-16):
# src/compiler/z42c.pipeline/tests/workspace_topo → artifacts/build/compiler/z42c.pipeline/tests/workspace_topo (no src pollution).
output_dir = "../../../../../artifacts/build/compiler/z42c.pipeline/tests/workspace_topo"
