# TsigReconcile 归一化对账单测（reconcile-tsig-from-runtime-sections / unify P2）。
[project]
name    = "z42c.project.test.reconcile"
version = "0.1.0"
kind    = "lib"

[sources]
include = ["**/*.z42"]

[dependencies]
"z42c.project" = "0.1.0"
"z42c.ir"      = "0.1.0"

[build]
output_dir = "../../../../../artifacts/build/compiler/z42c.project/tests/reconcile"
